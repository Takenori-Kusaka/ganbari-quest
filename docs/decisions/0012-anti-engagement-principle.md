# 0012. Anti-engagement 原則（滞在時間 = 価値毀損）

- **Status**: Accepted
- **Date**: 2026-04-21（#1345 で機能別細則 §6 を追記、2026-05-17 #2138 で rule-preset (penalty) 行を追記）
- **Related Issue**: #1309 (#1307 umbrella 派生 B9-ADR-ENGAGEMENT) / #1345 (#1328 umbrella C1-ANTIENGAGE-DETAIL) / #2138 (MP-3 マーケットプレイス rule-preset 完全実装)

## コンテキスト

本プロダクトは「親の願い（子供の現実活動の充実）の可視化」を存在目的とする。一方、従来の子供向けアプリ / ソシャゲ / 動画サービスは DAU × 滞在時間を KPI にした engagement 最適化設計を共有する（ガチャ / インフィニットスクロール / 通知過多 / 自動再生）。

この設計哲学は子供が **アプリと現実活動のどちらに時間を使うか** のトレードオフで現実活動を減らす方向に働く。親が購入を判断するプロダクトにおいて、子供の現実時間を奪う設計は親の購入動機と反する。

実際に、#1307 B9 で判明した通り、明示的 ADR が無かった結果、LP に「シールガチャ」文言が混入したまま (a) 実装は日 1 回 cap の `loginStamp` omikuji、(b) LP 上の描画は連続ガチャ的、という食い違いが 3 年以上放置された。哲学レベルでの判断原則として永続化し、将来の機能追加・コピー提案の即時審査に使う。

## 決定

### 1. アプリ滞在時間は価値毀損指標（親管理画面を除く）

子供側 UI は「記録する → 数秒で閉じる」を最短経路として設計する。セッション長を延ばす動機設計は採用しない。

### 2. 禁止する設計パターン（Anti-engagement 原則の下で原則不採用）

- 連続ガチャ / ランダム報酬を引き続けられる UI（2 回目以降のドロー動線を置かない）
- インフィニットスクロール
- 通知による再入店誘導（親の明示的同意がない限り）
- 動画視聴の連続自動再生
- 「次は何が出るか」心理を駆動するサプライズ系演出の濫用
- LP / 販促文言での「毎日遊べる」「連続ログインでレア獲得」等、滞在時間延伸を暗示する語彙

### 3. 許容する pattern

- **日 1 回 cap の短時間 interaction**（例: ログインボーナス、おみくじ — 数秒で完結するもの）
- **現実活動記録をトリガとする短時間フィードバック**（数秒の祝福演出）
- **親管理画面の滞在**（情報把握が目的なので制限なし）

### 4. KPI 設計方針

- DAU は追う（家族内での日次起動率）
- **セッション長は追わない** — むしろ短くなる方向が善
- 現実活動記録数 / チェックリスト達成率は追う（アプリ外時間の産物）

### 5. 新機能審査における使い方

新機能提案時は本 ADR に照らして「滞在時間を延ばす動機設計か」を必ず審査する。該当するなら採用しない（PMF 後でも原則変えない）。LP / パンフレット / プライシング戦略書等の販促文言も同じ審査対象。

### 6. 機能別 許容/禁止 UX 細則（#1345 追記）

原則（§1〜§3）の抽象度では各機能の境界が曖昧なため、PR / Issue レビュー時の即判定用に主要 5 機能の境界を確定させる。判定原則: **「1 日 1 タップ記録 → 数秒で完結 → 即離脱」動線に沿うか**。

| 機能 | 許容 UX | 禁止 UX |
|---|---|---|
| スタンプカード (L2 anti-habituation) | 1 日 1 回 cap のサプライズ獲得 → 結果提示 → 即閉じ / コレクションは親レポート側で総覧 | コレクション閲覧ページで滞在 / 並び替え・装飾カスタマイズ / 未取得枠を強調して取得欲を煽る UI |
| おみくじ（日 1 回 cap） | 1 タップ → 即結果 → 数秒で閉じる | 演出の長時間化 / 2 回目以降のドロー動線（再チャレンジ・スキップ禁止長演出） |
| バトル / クエスト | 現実活動記録完了への一回性報酬 | ステージ周回 / デイリーダンジョン型の反復戦闘 / スタミナ回復待機 UI |
| レベル・称号・実績 | ポイント履歴・現実活動の結果表現（受動的表示） | 収集目的の独立 UI / 称号コレクション閲覧ページ / ミッションリストから UI を駆動する導線 |
| ごほうびショップ | 交換動線（ポイント → ごほうび）のみ / 必要時のみ入る | ウィンドウショッピング滞在 / おすすめ表示による回遊 / 在庫変動のアニメ演出濫用 |
| Web Push 通知 (#1593) | **親端末のみ**送信 (parent / owner role の subscription)。日次 3 通 cap + 21:00-07:00 quiet hours。リマインダー・ストリーク警告・達成通知の 3 種のみ | **子端末への送信は構造的禁止**（`push_subscriptions.subscriber_role` カラムで subscribe API が child を 403 拒否 + notification-service 側で送信時に二重防御）。再入店誘導目的の通知連打 / 4 通目以降の cap 突破 / quiet hours 違反 |
| **rule-preset (penalty)** (#2138 MP-3) | **インフラのみ保持** — 取込時に warning 表示 + `settings.rule_preset_import_warnings` に audit log。実 preset 投入は本 ADR を supersede する個別 ADR 合意後の別 Issue でのみ許容。`/admin/settings/rules` に表示しない（既定 disable）。Octalysis Drive 8 (black hat)、子供のミス・遅刻・忘れ物に対する**減点 / ペナルティ系ルール** | **子供側 UI への penalty 表示・通知の構造的禁止**（`rule-preset-import-service.ts` で penalty ruleType を no-op warning にし、子供画面に reach するパス無し）。累積ペナルティ / quiet hours 違反通知 / 兄弟比較によるペナルティ / 失敗体験の連打表示 / 親のミス連動ペナルティ |
| **rule-preset (special)** (#2138 MP-3) | **将来枠** — 仕様未確定。取込時に warning 表示のみ (penalty と同形の no-op) | 仕様確定前の preset 投入 / 子供側 UI への露出 |

#### 適用範囲

- 子供側 UI (`(child)/[uiMode=uiMode]/`) 全て
- 親管理画面は本表の対象外（§3 の例外、情報把握目的のため滞在自体を許容）
- LP / パンフレットの該当機能コピーも同じ審査対象（ADR-0013 と併用）

#### レビュー時の運用

新機能 PR / LP コピー変更 PR で上記 5 機能 + rule-preset (penalty/special) 2 行に該当する UI を追加・変更する場合、レビュアーは「禁止 UX」列に該当しないか明示確認する。該当する場合は本 ADR を supersede する新 ADR を先に起票する（個別 PR で原則を崩さない）。

#### rule-preset (penalty) — 採用条件 / 既定 disable / opt-in 設定経路 (#2138)

**penalty ruleType を将来採用する場合の必須条件** (本 ADR §6 表の禁止 UX 列を満たさないかは別途審査):

1. **採用条件 (全件必須)**:
   - 親が**明示的に ON 操作**しない限り発火しない（既定値 `enabled: false`）
   - **子供 UI に penalty 文言を露出しない**（ポイント減算のみ、減算理由の通知禁止）
   - 累積ペナルティ禁止（同一日に複数 penalty rule が複合発火する設計を取らない）
   - quiet hours (21:00-07:00) 中は判定を行わない（リマインダー連打との合算を防ぐ）
   - 兄弟比較を含む rule は無条件で却下

2. **既定 disable の構造的強制**:
   - `rule-preset-import-service.ts` で penalty ruleType を no-op warning にし、import 操作だけでは active 状態にならない
   - 親が `/admin/settings/rules` 画面で**明示 ON 切替を行わない限り**、`bonus-hook-service.ts` 相当の penalty-hook (将来) は発火しない

3. **opt-in 設定経路**:
   - 親管理画面 (`/admin/settings/rules`) で個別 penalty preset の ON/OFF を切替
   - opt-in 前に**警告モーダル必須** (ADR-0012 §6 細則に該当する文言で「子供の失敗体験を強化する設計です」を表示し親同意 chip を取る)
   - 1 件 ON → 即時反映ではなく **24h 待機期間** を設ける（親が冷静になる時間。Pre-PMF では未実装、本格採用時に追加）

4. **ADR supersede 必要**:
   - 上記 1-3 を満たす penalty preset 実装は本 ADR の射程内だが、**実 penalty preset JSON 投入**は本 ADR を supersede する新 ADR で個別合意（preset ごとに）
   - 「N 件以内なら本 ADR の範囲内」のような数量例外を作らない

## 結果

- 新機能追加時の判断軸に「子供の現実時間を奪う設計か」が恒久的に追加される
- LP / 販促文言のレビュー時に射幸性・engagement 増大系語彙が即検出対象になる（`scripts/measure-lp-dimensions.mjs` の `forbiddenTerms` 拡張を #1313 で実装）
- KPI ダッシュボードから子供側セッション長指標を外す（親側のみ残す）
- ガチャ系 UI 提案 / 通知連打系 UI 提案は初動で却下できる

### トレードオフ

- 一般的な子供向けアプリのベストプラクティス（例: daily streak を視覚的に誇張する）の一部は採用できず、短期 retention は相対的に弱い
- 「親向けに販促しにくい代わりに子供が自発的に長時間使う機能」も不採用となり、一部 CV 機会を放棄する

## Alternatives considered

- **一般的な engagement 重視戦略**: 本プロダクトの親層購入動機と反するため不採用
- **無制限自由設計（原則なし）**: 滞在時間最適化の誘惑に構造的に勝てず、個別判断で崩れるため不採用

## 関連

- ADR-0010（Pre-PMF スコープ判断）— スコープ絞り込みの判断基準を補強
- 後続: ADR-0013 (LP 実装 SSOT、#1310) — 「LP が先、実装が後」の reconcile を義務化し本 ADR の射程（販促文言）を構造的に担保
- Umbrella: #1307 (B9 LP「ガチャ」呼称と実装の不整合解消) / #1328 (C1 コアループ再定義)
- #1345 (C1-ANTIENGAGE-DETAIL): 本 ADR §6 機能別細則の追記元
- 前駆議論: 2026-04-21 PO セッション B9 ブレスト / 2026-04-21 C1 ブレスト
