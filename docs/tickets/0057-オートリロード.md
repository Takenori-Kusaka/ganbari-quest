# 0057 1分間隔のオートリロード

### ステータス

`Done`

---

### 概要

子供画面で1分に1回自動リロードし、親が管理画面で変更した活動・設定を反映する。

### 対応方針

- 子供用レイアウト (`src/routes/(child)/+layout.svelte`) に `setInterval` + `invalidateAll()` を追加
- 間隔: 60秒
- ページがバックグラウンドの場合はスキップ（`document.hidden`）
- ダイアログ表示中はスキップ（記録中・おみくじ表示中に画面が変わると混乱する）
