# OSS汎用化とドキュメント整備

### ステータス

`Done`

---

### 概要

日下家専用のハードコードを排除し、不特定多数の家庭がカスタマイズして利用できるOSSとして整備する。初期セットアップウィザードまたはドキュメントにより、任意の家庭構成でシステムを立ち上げられるようにする。

### 背景・動機

現在のシステムは日下家の子供5名がseed.tsにハードコードされており、PINも固定値`1234`で初期化される。OSSとしてリリースするためには:
- 家族固有データの除去（子供名、人数、年齢等）
- 初期セットアップフローの整備（対話的 or ドキュメント）
- 環境構築手順の詳細ドキュメント化
- ライセンス・コントリビューションガイドの整備

が必要。

**要件1**: OSS汎用化

### ゴール

#### コード変更

- [ ] seed.tsから日下家固有の子供データを除去し、サンプルデータ or 空で初期化する方式に変更
- [ ] 初回起動時にWeb UIから子供の登録ができるセットアップウィザードを実装
- [ ] `.env.example` を拡充（全環境変数の説明付き、Google Drive関連も含む）
- [ ] デフォルトPINの初回変更を強制するフロー（初回ログイン時にPIN変更画面へ）
- [ ] ORIGIN環境変数をホスト名から自動検出するフォールバックを追加

#### ドキュメント

- [ ] `INSTALLATION.md` を作成
  - 前提条件（Node.js 22+, npm）
  - クイックスタート（Docker方式）
  - 手動セットアップ（ソースビルド方式）
  - 環境変数リファレンス
  - 初期データ投入手順
  - SSL/HTTPS設定（任意）
- [ ] `CONTRIBUTING.md` を作成（開発者向け）
  - 開発環境構築
  - コーディング規約（CLAUDE.md参照）
  - PR手順
  - テスト実行方法
- [ ] `README.md` を刷新
  - プロジェクト概要（スクリーンショット付き）
  - 主要機能一覧
  - クイックスタート（3ステップ程度）
  - ライセンス表記
- [ ] `LICENSE` ファイルを追加（MIT推奨）
- [ ] `.env.example` に全変数を網羅

#### 設定の外部化

- [ ] アプリ名（「がんばりクエスト」）をsettingsテーブル or 環境変数で変更可能にする
- [ ] レベルテーブル（タイトル名）をDB設定 or JSONで外部定義化
- [ ] テーマカラーの追加が容易な構造であることを確認

### 技術方針

#### seed.ts の変更方針

```typescript
// Before: 日下家固有
const childrenData = [
  { nickname: 'ゆうきちゃん', age: 4, ... },
  ...
];

// After: 空（初回起動時にWeb UIから登録）
// activitiesとachievementsのマスタデータのみseed
// childrenテーブルは空のまま起動
```

#### セットアップウィザード

- `/setup` ルートを追加（childrenテーブルが空の場合のみアクセス可能）
- ステップ1: 子供の登録（名前、生年月日、テーマ、UIモード）
- ステップ2: 管理PIN設定
- ステップ3: 完了→ホーム画面へリダイレクト

### 作業メモ

- 現在の日下家固有データ:
  - `seed.ts` L28-32: 子供5名のニックネーム・年齢・テーマ
  - `seed.ts`: デフォルトPIN `1234`
- 活動マスタ（174件）と実績テンプレートは汎用的なので維持
- market_benchmarksデータも汎用的（年齢別の統計ベンチマーク）

### 成果・結果

### 残課題・次のアクション

- 多言語対応（i18n）の検討（将来）
- アプリのカスタムブランディング機能（ロゴ変更等）
