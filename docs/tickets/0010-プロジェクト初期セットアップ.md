# プロジェクト初期セットアップ

### ステータス

`Done`

---

### 概要

SvelteKit プロジェクトの初期セットアップ。パッケージインストール、設定ファイル、ディレクトリ構成の整備。

### 背景・動機

設計ドキュメント完了後、実装を開始するための土台を整える。

### ゴール

- [x] SvelteKit 2 プロジェクト作成 (`npx sv create`)
- [x] 依存パッケージインストール（Ark UI, Drizzle ORM, Tailwind CSS, Biome, Vitest, Playwright）
- [x] TypeScript strict 設定
- [x] Biome 設定 (`biome.json`)
- [x] Drizzle ORM 設定 (`drizzle.config.ts`)
- [x] ディレクトリ構成の整備（AGENTS.md の構成に準拚）
- [x] `.env.example` 作成
- [x] `.gitignore` 整備
- [x] 初回ビルド・テスト実行の確認

### 作業メモ

- CLAUDE.md / AGENTS.md の内容に沿って構成
- NUCサーバーへのデプロイ設定は別チケット

### 成果・結果

- SvelteKit 2 + Svelte 5 (Runes) プロジェクト作成完了
- package.json に全依存パッケージ設定済み
- tsconfig.json (strict: true), biome.json, drizzle.config.ts, vite.config.ts, playwright.config.ts 全設定完了
- AGENTS.md 準拚のディレクトリ構成整備済み
- .env.example, .gitignore 作成済み
- 初回ビルド・テスト実行確認済み

### 残課題・次のアクション

- 各機能の実装チケットへ展開
