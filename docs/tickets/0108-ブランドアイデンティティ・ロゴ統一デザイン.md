# #0108 ブランドアイデンティティ・ロゴ・Favicon 統一デザイン

## ステータス: Done
## 優先度: 中
## 種別: デザイン改善

---

## 概要

現在の Favicon やアプリ内のビジュアル要素は、「子供の成長を助けるサービス」というコンセプトを適切に表現できていない。SaaS展開やログイン画面の整備に向けて、ブランドアイデンティティを統一的に設計し、Favicon・ロゴ・各画面での一貫したビジュアル表現を確立する。

## 現状の問題

### 1. Favicon が不適切

- 現在: 青い角丸四角に黄色い星＋顔（`static/favicon.svg`）
- 「子供の成長を助ける」サービスイメージと結びつかない
- 16x16px で視認性が低い

### 2. ロゴが存在しない

- ロゴ専用の画像ファイル・コンポーネントが一切ない
- ログイン画面では 👨‍👩‍👧 絵文字で代替している
- ヘッダーにもロゴ要素がない

### 3. ブランドガイドラインが未整備

- UI設計書（06-UI設計書.md）にカラーパレット・タイポグラフィはあるが、ロゴ・ビジュアルアイデンティティの定義がない
- 各画面のアイコン使用が絵文字ベースで統一感に欠ける

### 4. 画面ごとの不統一

| 画面 | 現在のビジュアル | 問題 |
|------|----------------|------|
| ブラウザタブ | 星キャラの Favicon | サービス内容が伝わらない |
| ログイン画面 | 👨‍👩‍👧 絵文字 + ひらがなタイトル | ロゴ不在、プロダクト感が弱い |
| 子供ホーム画面 | アバター + ポイント表示 | ブランド要素なし |
| 親管理画面 | テキストのみのヘッダー | ブランド要素なし |
| PWA インストール時 | Favicon がそのまま使われる | 専用のアプリアイコンが必要 |

## 設計方針

### ブランドコンセプト

**「がんばりクエスト」= 子供の成長を冒険に変える**

| 要素 | 方向性 |
|------|-------|
| キーワード | 成長・冒険・がんばり・達成・家族 |
| トーン | 温かい・楽しい・信頼感・やさしい |
| ターゲット印象 | 子供: わくわく / 親: 安心・信頼 |

### ロゴデザイン要件

#### メインロゴ
- 「がんばりクエスト」のサービス名を含むロゴタイプ
- シンボルマーク + ワードマークの組み合わせ
- 子供の成長・冒険・達成感を象徴するモチーフ
  - 候補: 芽吹き🌱、星⭐、旗🚩、山の頂上、盾（クエスト）、上昇する矢印
- カラー: 既存のデザイントークンと調和（primary blue + accent yellow/green）

#### バリエーション
| 種別 | 用途 | サイズ |
|------|------|-------|
| フルロゴ | ログイン画面、ホームページ | 横幅 200px〜 |
| コンパクトロゴ | ヘッダー、ナビゲーション | 横幅 120px〜 |
| シンボルマーク | Favicon、PWAアイコン、OGP | 32〜512px |
| モノクロ版 | 印刷、低解像度環境 | 各サイズ |

### Favicon 要件

- 16x16, 32x32, 180x180（Apple Touch）, 192x192, 512x512（PWA）
- シンボルマークを単独で使用
- 小サイズでも識別可能なシンプルな形状
- SVG + PNG の両形式

### 各画面への適用

| 画面 | 適用内容 |
|------|---------|
| `src/app.html` | Favicon・OGPメタタグの更新 |
| `(parent)/login/+page.svelte` | ロゴ表示（絵文字 → メインロゴに置換） |
| `src/lib/ui/components/Header.svelte` | コンパクトロゴの追加（子供画面） |
| `(parent)/+layout.svelte` | 親管理画面ヘッダーへのロゴ表示 |
| `static/manifest.json` | PWA アイコンの更新 |
| ホームページ（将来） | フルロゴの使用 |

## 成果物

### 必須
- [x] ブランドガイドライン文書（`docs/design/15-ブランドガイドライン.md`）
  - ロゴの使用規定（最小サイズ、余白、禁止例）
  - カラーパレットとロゴの組み合わせ
  - フォント指定とロゴタイプのバランス
- [x] シンボルマーク SVG（`static/favicon.svg` — 盾+星モチーフ）
- [x] Favicon 一式（SVG + PNG 各サイズ: 32, 180, 192, 512, maskable）
- [x] メインロゴ SVG（`static/logo.svg`, `static/logo-compact.svg`）
- [x] ログイン画面のロゴ適用（auth/login, auth/signup, (parent)/login）

### 推奨
- [ ] OGP 画像（SNS共有用 1200x630px）— 将来対応
- [ ] PWA スプラッシュスクリーン用画像 — 将来対応
- [x] ロゴコンポーネント（`src/lib/ui/components/Logo.svelte`）
  - サイズ・バリアント切り替え対応（symbol/full/compact）
- [x] 子供画面・親画面へのロゴ表示（管理画面ヘッダーに追加）

## 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `static/favicon.svg` | シンボルマークに差し替え |
| `static/favicon-32x32.png` | 新デザインで再生成 |
| `src/app.html` | Favicon参照の更新、OGPメタタグ追加 |
| `src/routes/(parent)/login/+page.svelte` | 👨‍👩‍👧 → ロゴに置換 |
| `src/lib/ui/components/Header.svelte` | ロゴ要素の追加検討 |
| `docs/design/` | ブランドガイドライン文書の新規追加 |

## テスト

- [x] Favicon が各ブラウザで正しく表示されること（SVG + PNG生成済み）
- [x] PWA インストール時にアプリアイコンが正しく表示されること（192/512/maskable PNG生成済み）
- [x] ログイン画面でロゴが適切に表示されること（E2Eテスト 208/208 通過）
- [ ] ダークモード/ライトモードでロゴの視認性が確保されていること（目視確認 — 将来対応）
- [x] 16x16px の最小サイズでもシンボルマークが識別可能であること（盾+星のシンプルな形状）

## 注意事項

- 既存のカラーパレット（UI設計書のデザイントークン）との調和を優先
- 子供向け画面と親向け画面で同一ロゴを使いつつ、周囲のトーンで差別化
- SaaS展開時のホームページ（LP）デザインとの一貫性を意識

## 関連チケット

- #0066 OAuth認証・家族共有機能（ログイン画面の再設計に影響）
- #0107 グローバライゼーション・多言語化対応（ロゴにテキストを含む場合の考慮）

## 成果・結果

- コミット: 05924b7
- シンボルマーク: 盾(Quest)+星(Achievement)モチーフ、ブルー+ゴールドグラデーション
- Logo.svelte: symbol/full/compact の3バリアント対応
- Favicon/PWAアイコン: `scripts/generate-icons.cjs` で自動生成
- ブランドガイドライン: `docs/design/15-ブランドガイドライン.md`
- OGP画像・PWAスプラッシュは将来対応（LPデザイン時に合わせて作成）
