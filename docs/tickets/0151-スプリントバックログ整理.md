# スプリントバックログ整理・未着手 Ready チケット対応計画

## Status: Done

### GitHub Copilot 対応: 可能

> Ready/Waiting になっているが着手されていないチケットを整理し、優先順位・依存関係・実施可否を整理したスプリント計画。

---

## 調査日: 2026-03-27

## 対応状況サマリー

### ✅ 実装済み・ステータス修正済み

| # | チケット | 確認内容 | ステータス更新 |
|---|---------|---------|--------------|
| #0149 | HPヘッダーログインボタン文字色バグ修正 | `site/index.html` に `.header-nav a:not(.btn)` 適用済み | Ready → Done（済） |
| #0150 | 親管理画面ヘッダーロゴ適用 | `admin/+layout.svelte` に `<Logo variant="compact" />` 適用済み | Ready → Done（本チケット確認時に修正） |

---

## 着手待ちチケット一覧

### 🟡 Ready（着手可能）

#### #0108 — ブランドアイデンティティ・ロゴ統一デザイン【再設計】
| 項目 | 値 |
|------|-----|
| 優先度 | 高 |
| 規模 | 大（AI生成比較 + SVG再作成） |
| ブロッカー | なし |
| 担当推奨 | デザイナー（Gemini 2.0 Flash Image Generation）+ 開発 |
| 備考 | 現行の盾+星ロゴはマーケティング観点で不合格と判定。6コンセプトをAI生成・比較選定後にSVG再構築 |

**次アクション**: Gemini に各コンセプトA〜Fの画像生成プロンプトを送信し、`docs/design/logo-candidates/` に保存。評価シートで採点。

---

#### #0110 — 開発環境 Vite+ 統合ツールチェーン移行
| 項目 | 値 |
|------|-----|
| 優先度 | 低 |
| 規模 | 中（npm更新・設定ファイル変更） |
| ブロッカー | **npm パッケージ追加 → 着手前に承認必要（AGENTS.md）** |
| リスク | Vite+ は Alpha（v0.1.x）。本番影響あり |
| 備考 | Vite 8 + Rolldown ビルド高速化が主目的。`better-sqlite3` の CJS インターロップ確認が必須前提 |

**次アクション**: 実施可否の確認を取ってから着手。Alpha 版のため、実施前に Vite+ の最新バージョンと安定性を再確認。`npx vite@8 --version` で Vite 8 単独アップグレードから試みることを推奨。

---

#### #0132 — 家族グループ削除・データ破棄
| 項目 | 値 |
|------|-----|
| 優先度 | 低（MVP後） |
| 規模 | 大（DynamoDB + S3 + Cognito + Stripe 連携） |
| ブロッカー | Stripe サブスクリプション解約 API の確認 |
| 備考 | 30日猶予期間付き解約フロー。テナント全データ削除を伴うため、**誤操作防止の二重確認UI**必須 |

**次アクション**: 設計レビュー後に実装着手。Stripe のサブスクリプション解約 API (`stripe.subscriptions.cancel`) の動作確認から始める。

---

#### #0148 — トップページ訴求力リニューアル
| 項目 | 値 |
|------|-----|
| 優先度 | 高（マーケティング直結） |
| 規模 | 大（`site/index.html` 全面書き換え） |
| ブロッカー | #0108（新ロゴ）が完成していると理想だが、先行して着手可能 |
| 備考 | 現行は機能リストベース。「親の痛みから始まる」感情共鳴型に構成変更。`pain-scenes` → `solution` → `before-after` → `features` → `social-proof` → `cta` の構成 |

**次アクション**: コピーライティングを先行して作成（ペルソナ P1: 田中ゆかりの痛みシーン）。ロゴは現行 favicon.svg 暫定使用で着手可能。

---

### 🔵 Waiting（ユーザー確認待ち・設計待ち）

| # | チケット | 待機理由 |
|---|---------|---------|
| #0145 | おみくじ演出デザインリニューアル | デザイン方針確定待ち。#0108 ロゴ再設計と並行検討推奨 |
| #0146 | ステータス★評価とスコアの不一致修正 | A案（割合ベース）/ B案（割合+偏差値ラベル）の方針選択待ち |
| #0147 | レベル称号カスタマイズ機能 | DBスキーマ変更を伴うため着手前に確認が必要 |

---

### 📋 Backlog（将来対応）

| # | チケット | 備考 |
|---|---------|------|
| #0071 | プライシング戦略・料金プラン設計 | Stripe連携（#0131）完了後に着手 |
| #0076 | LP・HP・Discord構築 | #0148（LP改善）で一部カバー |
| #0077 | マーケティング素材・パンフレット | #0108（ロゴ確定）後に着手 |
| #0078 | リリース判定・運用体制構築 | 全機能完成後 |
| #0135 | Browser-Use-CLI 視覚デザインテスト | 実験的機能。優先度低 |
| #0136 | 親管理画面こども管理UX統一 | UX改善。MVP後 |

---

## 推奨実施順序

```
Week 1（今週）:
  1. #0108 ロゴ再設計 Phase 1: Gemini で6コンセプト生成・比較
  2. #0148 トップページ: コピーライティング草案作成（pain-scenes, solution）
  3. #0146 ★評価修正: A案/B案の方針確定 → 実装（小規模）

Week 2:
  4. #0148 トップページ: HTML/CSS 実装
  5. #0108 ロゴ再設計 Phase 2: 選定コンセプトのSVG化・アセット再生成
  6. #0145 おみくじ演出: デザイン方針確定後に実装着手

Week 3:
  7. #0147 称号カスタマイズ: DBスキーマ確認後に実装
  8. #0110 Vite+ 移行: 承認取得後、Vite 8 アップグレードから段階的に実施
  9. #0132 家族グループ削除: 設計レビュー後に実装着手
```

---

## ステータス変更ログ（本チケット確認時）

| # | チケット | 変更前 | 変更後 | 理由 |
|---|---------|--------|--------|------|
| #0150 | 親管理画面ヘッダーロゴ適用 | Ready | Done | `admin/+layout.svelte` に既に `<Logo variant="compact" />` 実装済みを確認 |
| #0149 | HPヘッダーログインボタン文字色バグ修正 | (Ready) | Done | `site/index.html` に `.header-nav a:not(.btn)` 既に適用済みを確認 |
