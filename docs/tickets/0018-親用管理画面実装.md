# 親用管理画面実装

## Status: Done

### GitHub Copilot 対応: 可能

> 管理画面のCRUD系UIはCopilot対応可能

---

### 概要

親が使う管理画面（PIN認証、活動管理、ポイント変換、表示/非表示管理）を実装する。

### 背景・動機

親が子供の活動を管理し、ポイント変換等を行うための管理画面。

### ゴール

- [x] `src/routes/(parent)/` レイアウト（admin/+layout.svelte）
- [x] PIN認証画面・ロジック（hooks.server.ts ガード済み, login画面既存）
- [x] 活動管理画面（追加・表示/非表示切替）
- [x] ポイント変換画面（500P単位）
- [x] 活動表示/非表示管理画面（活動管理に統合）
- [x] 子供のステータス確認画面（親向け詳細表示）
- [x] 特別報酬付与画面（テンプレート選択 → カスタマイズ → 付与）
- [x] E2Eテスト（別チケットで対応）

### 前提条件

- #0022 認証・ユーザー管理基盤（PIN認証が前提）

### 関連チケット

- #0025 特別報酬システム（親用UIが本チケットに含まれる）
- #0028 活動マスタ拡充（活動管理UIが本チケットに含まれる）

### 作業メモ

- PINコードはハッシュ化して保存
- 管理画面は通常のWebフォームUI（アイコン中心でなくてよい）
- 特別報酬の付与UIは親のUXを最優先（3タップ以内で完了）
- 活動追加は、自然言語や音声入力による追加を可能とし生成AIにより適切なカテゴリやポイントを自動推定する機能も検討(もちろん確実な入力も可能にすること)

### 成果・結果

- `admin/+layout.svelte`: ヘッダ + 5項目ナビ + max-w-4xlコンテナ
- `admin/+page.svelte`: ダッシュボード（サマリーカード + 子供一覧 + クイックアクション）
- `admin/children/+page.svelte`: 子供選択 + 詳細表示（ステータス・偏差値・実績・ログ）
- `admin/activities/+page.svelte`: カテゴリフィルタ + 追加フォーム + 表示/非表示トグル
- `admin/points/+page.svelte`: 子供選択 + 500P単位変換
- `admin/rewards/+page.svelte`: 3ステップ（子供選択 → テンプレート → 確認付与）
- 全ページ a11y対応済み、TypeScriptエラー0件

### 残課題・次のアクション

- 活動の編集・削除機能（現在は追加と表示/非表示のみ）
- E2Eテスト追加
- 自然言語入力による活動追加（AI推定）は将来対応

