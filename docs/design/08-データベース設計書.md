# がんばりクエスト データベース設計書

| 項目 | 内容 |
|------|------|
| 版数 | 5.3 |
| 作成日 | 2026-02-19 |
| 更新日 | 2026-04-26 |
| 作成者 | 日下武紀 |

---

## 1. 設計方針

### デュアルバックエンド構成

本アプリケーションは2つのデプロイ環境に対応するため、DB バックエンドを抽象化している:

| 環境 | DB | 用途 |
|------|-----|------|
| AWS Lambda（本番 SaaS） | DynamoDB | シングルテーブル設計、マルチテナント対応 |
| NUC サーバー（ローカル） | SQLite | 単一ファイルDB、単一テナント |

### アーキテクチャ

```
src/lib/server/db/
  ├── interfaces/          # リポジトリインターフェース（17ファイル）
  ├── dynamodb/            # DynamoDB 実装
  │   ├── keys.ts          # PK/SK 生成ヘルパー
  │   ├── counter.ts       # ID 自動採番
  │   └── *-repo.ts        # エンティティ別リポジトリ
  ├── sqlite/              # SQLite 実装（Drizzle ORM）
  │   └── *-repo.ts
  ├── factory.ts           # 環境変数で実装を切り替え
  ├── schema.ts            # SQLite スキーマ定義
  └── *-repo.ts            # ファサード（factory からの re-export）
```

- `DB_BACKEND` 環境変数で `dynamodb` / `sqlite` を切り替え
- 全リポジトリに `tenantId: string` パラメータが必須
- SQLite 実装では tenantId を受け取るが内部では無視（単一テナント）

### SQLite 固有の方針

- **Drizzle ORM**: TypeScript型安全なスキーマ定義
- **WAL モード**: 読み書き並行性を向上
- **バックアップ**: 日次で git push（DBファイルのコピー）

---

## 2. ER図

> テーブル数が多いため、主要な関連のみを図示。全テーブル定義はセクション3を参照。

```
┌───────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  categories   │     │    activities     │     │  stamp_masters   │
│───────────────│     │──────────────────│     │──────────────────│
│ id (PK)       │◄──┐ │ id (PK)          │     │ id (PK)          │
│ code (UNIQUE) │   │ │ name             │     │ name             │
│ name          │   │ │ category_id (FK) │──┘  │ emoji            │
│ icon          │   │ │ icon             │     │ rarity           │
│ color         │   │ │ base_points      │     └────────┬─────────┘
└───────────────┘   │ │ source           │              │
                    │ │ ...              │              │
                    │ └────────┬─────────┘              │
                    │          │                         │
┌──────────────┐    │          │                         │
│   children   │    │          │                         │
│──────────────│    │          │                         │
│ id (PK)      │    │          │                         │
│ nickname     │    │          │                         │
│ age          │    │          │                         │
│ ui_mode      │    │          │                         │
│ theme        │    │          │                         │
│ user_id      │    │          │                         │
│ ...          │    │          │                         │
└──────┬───────┘    │          │                         │
       │            │          │                         │
       │    ┌───────┘──────────┘                         │
       │    │    │                                       │
┌──────▼────▼──────┐  ┌──────────────────────┐  ┌──────▼─────────────┐
│  activity_logs   │  │  child_achievements  │  │   stamp_entries    │
│──────────────────│  │──────────────────────│  │────────────────────│
│ child_id (FK)    │  │ child_id (FK)        │  │ card_id (FK)       │
│ activity_id (FK) │  │ achievement_id (FK)  │  │ stamp_master_id(FK)│
│ points           │  │ milestone_value      │  │ slot               │
│ streak_days      │  │ unlocked_at          │  │ login_date         │
│ recorded_date    │  └──────────────────────┘  └────────────────────┘
│ cancelled        │                                     ▲
└──────────────────┘  ┌──────────────────────┐  ┌────────┴───────────┐
                      │  special_rewards     │  │   stamp_cards      │
┌──────────────────┐  │──────────────────────│  │────────────────────│
│   point_ledger   │  │ child_id (FK)        │  │ child_id (FK)      │
│──────────────────│  │ title               │  │ week_start         │
│ child_id (FK)    │  │ points              │  │ status             │
│ amount           │  │ category            │  └────────────────────┘
│ type             │  └──────────────────────┘
│ reference_id     │                            ┌────────────────────────┐
└──────────────────┘  ┌──────────────────────┐  │ checklist_templates    │
                      │    statuses          │  │────────────────────────│
┌──────────────────┐  │──────────────────────│  │ child_id (FK)          │
│ status_history   │  │ child_id (FK)        │  │ name                   │
│──────────────────│  │ category_id (FK)     │  │ points_per_item        │
│ child_id (FK)    │  │ total_xp             │  └───────────┬────────────┘
│ category_id (FK) │  │ level                │              │
│ value            │  │ peak_xp              │  ┌───────────▼────────────┐
│ change_amount    │  └──────────────────────┘  │ checklist_template_items│
│ change_type      │                            │────────────────────────│
└──────────────────┘  ┌──────────────────────┐  │ template_id (FK)       │
                      │  parent_messages     │  │ name                   │
┌──────────────────┐  │──────────────────────│  └────────────────────────┘
│ season_events    │  │ child_id (FK)        │
│──────────────────│  │ message_type         │  ┌────────────────────────┐
│ code (UNIQUE)    │  │ stamp_code           │  │   settings (KVS)       │
│ event_type       │  │ body                 │  │────────────────────────│
│ start_date       │  └──────────────────────┘  │ key (PK)               │
│ end_date         │                            │ value                  │
└──────┬───────────┘  ┌──────────────────────┐  └────────────────────────┘
       │              │ sibling_challenges   │
┌──────▼───────────┐  │──────────────────────│
│child_event_      │  │ challenge_type       │
│ progress         │  │ start_date           │
│──────────────────│  │ end_date             │
│ child_id (FK)    │  └──────┬───────────────┘
│ event_id (FK)    │         │
│ status           │  ┌──────▼───────────────┐
└──────────────────┘  │ sibling_challenge_   │
                      │  progress            │
                      │──────────────────────│
                      │ challenge_id (FK)    │
                      │ child_id (FK)        │
                      │ current_value        │
                      │ target_value         │
                      └──────────────────────┘
```

---

## 3. テーブル定義

### children

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| nickname | TEXT | NO | - | 表示名（ひらがな） |
| age | INTEGER | NO | - | 年齢 |
| birth_date | TEXT | YES | NULL | 誕生日（YYYY-MM-DD） |
| theme | TEXT | NO | 'pink' | テーマ名 |
| ui_mode | TEXT | NO | 'preschool' | UIモード（baby/preschool/elementary/junior/senior）。`getDefaultUiMode(age)` で導出 |
| ui_mode_manually_set | INTEGER | NO | 0 | 手動設定フラグ（0=自動, 1=保護者が明示指定。age 変更時の自動再計算をスキップ） |
| avatar_url | TEXT | YES | NULL | アバター画像URL |
| active_title_id | INTEGER | YES | NULL | 装備中の称号ID |
| display_config | TEXT | YES | NULL | 表示設定（JSON: cardSize, itemsPerCategory, collapsible） |
| user_id | TEXT | YES | NULL | Cognito ユーザーID（SaaS モード） |
| birthday_bonus_multiplier | REAL | NO | 1.0 | 誕生日ボーナス倍率 |
| last_birthday_bonus_year | INTEGER | YES | NULL | 最後に誕生日ボーナスを適用した年 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |
| _sv | INTEGER | YES | NULL | スキーマバージョン（Lazy Migration 用） |
| is_archived | INTEGER | NO | 0 | アーカイブフラグ（#783: トライアル終了時に超過リソースを非表示化） |
| archived_reason | TEXT | YES | NULL | アーカイブ理由（例: 'trial_expired'） |

### categories

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | PK | 主キー |
| code | TEXT | NO | - | カテゴリコード（UNIQUE） |
| name | TEXT | NO | - | カテゴリ名（例: うんどう、べんきょう） |
| icon | TEXT | YES | NULL | アイコン |
| color | TEXT | YES | NULL | カテゴリカラー |

### activities

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| name | TEXT | NO | - | 活動名（ひらがな） |
| category_id | INTEGER | NO | - | FK → categories.id |
| icon | TEXT | NO | - | アイコン識別子 |
| base_points | INTEGER | NO | 5 | 基本ポイント |
| age_min | INTEGER | YES | NULL | 対象最小年齢 |
| age_max | INTEGER | YES | NULL | 対象最大年齢 |
| is_visible | INTEGER | NO | 1 | 表示フラグ (0/1) |
| daily_limit | INTEGER | YES | NULL | 1日の記録上限 |
| sort_order | INTEGER | NO | 0 | 表示順 |
| source | TEXT | NO | 'seed' | データソース（seed/custom/import/pack） |
| grade_level | TEXT | YES | NULL | 対象学年 |
| subcategory | TEXT | YES | NULL | サブカテゴリ |
| description | TEXT | YES | NULL | 活動の説明 |
| name_kana | TEXT | YES | NULL | 読み仮名 |
| name_kanji | TEXT | YES | NULL | 漢字表記 |
| trigger_hint | TEXT | YES | NULL | 記録のきっかけヒント |
| is_main_quest | INTEGER | NO | 0 | メインクエストフラグ（1=有効、最大3つ） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| is_archived | INTEGER | NO | 0 | アーカイブフラグ（#783: トライアル終了時に超過リソースを非表示化） |
| archived_reason | TEXT | YES | NULL | アーカイブ理由（例: 'trial_expired'） |

### activity_logs

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| activity_id | INTEGER | NO | - | FK → activities.id |
| points | INTEGER | NO | - | 基本ポイント |
| streak_days | INTEGER | NO | 1 | 連続記録日数 |
| streak_bonus | INTEGER | NO | 0 | ストリークボーナスポイント |
| recorded_date | TEXT | NO | - | 記録日（YYYY-MM-DD） |
| recorded_at | TEXT | NO | CURRENT_TIMESTAMP | 記録日時 |
| cancelled | INTEGER | NO | 0 | キャンセル済みフラグ |

**ユニーク制約:** (child_id, activity_id, recorded_date) ※同一活動は1日1回まで

**インデックス:**
- `idx_activity_logs_child_date` ON (child_id, recorded_date)
- `idx_activity_logs_activity` ON (activity_id)
- `idx_activity_logs_streak` ON (child_id, activity_id, recorded_date)

### point_ledger

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| amount | INTEGER | NO | - | 金額（正: 獲得、負: 消費） |
| type | TEXT | NO | - | 種別: earn / spend / convert / cancel |
| description | TEXT | YES | NULL | 説明 |
| reference_id | INTEGER | YES | NULL | 関連するログID等 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:**
- `idx_point_ledger_child` ON (child_id, created_at)

### statuses

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| category_id | INTEGER | NO | - | FK → categories.id |
| total_xp | INTEGER | NO | 0 | 累計経験値（XP） |
| level | INTEGER | NO | 1 | 現在レベル（1〜99） |
| peak_xp | INTEGER | NO | 0 | 最高到達XP（衰退フロア計算用） |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 最終更新日時 |
| _sv | INTEGER | YES | NULL | スキーマバージョン（Lazy Migration 用） |

**ユニーク制約:** (child_id, category_id)

> **#0255 変更**: `value REAL` (0-100スケール) → `total_xp INTEGER` + `level INTEGER` + `peak_xp INTEGER` に移行。ポイント＝XPとして直接蓄積。99レベルRPGスタイルプログレッシブカーブ。

### status_history

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| category_id | INTEGER | NO | - | FK → categories.id |
| value | REAL | NO | - | 変更後の値 |
| change_amount | REAL | NO | - | 変更量（正: 増加、負: 減少） |
| change_type | TEXT | NO | - | increase / decrease / evaluation / daily_decay |
| recorded_at | TEXT | NO | CURRENT_TIMESTAMP | 記録日時 |

**インデックス:**
- `idx_status_history_child_cat` ON (child_id, category_id, recorded_at)

### evaluations

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| week_start | TEXT | NO | - | 評価週の開始日 |
| week_end | TEXT | NO | - | 評価週の終了日 |
| scores_json | TEXT | NO | - | カテゴリ別評価スコア（JSON） |
| bonus_points | INTEGER | NO | 0 | ボーナスポイント |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**scores_json 例:**
```json
{
  "うんどう": { "count": 5, "points": 25, "statusDelta": 2.0 },
  "べんきょう": { "count": 3, "points": 15, "statusDelta": 1.0 }
}
```

### market_benchmarks

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| age | INTEGER | NO | - | 対象年齢 |
| category_id | INTEGER | NO | - | FK → categories.id |
| mean | REAL | NO | - | 平均値 |
| std_dev | REAL | NO | - | 標準偏差 |
| source | TEXT | YES | NULL | データソース |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (age, category_id)

### settings

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| key | TEXT | NO | - | 設定キー（主キー） |
| value | TEXT | NO | - | 設定値 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**使用するキー:**
| key | 説明 |
|-----|------|
| pin_hash | PINコードのbcryptハッシュ |
| session_token | 現在のセッショントークン |
| session_expires_at | セッション有効期限 |
| pin_failed_attempts | PIN失敗回数 |
| pin_locked_until | ロックアウト解除時刻 |

### level_titles（⚠️ #321 で廃止予定）

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| level | INTEGER | NO | - | レベル（1〜10） |
| custom_title | TEXT | NO | - | カスタム称号名 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (tenant_id, level)

テナント（家庭）単位でレベル称号をカスタマイズ。レコードが存在しないレベルは `LEVEL_TABLE` のデフォルト称号を使用。

### child_custom_voices

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | 子供ID（FK） |
| scene | TEXT | NO | 'complete' | 再生シーン（complete/levelup/login） |
| label | TEXT | NO | - | ラベル（「お母さんの声」等） |
| file_path | TEXT | NO | - | ストレージキー |
| public_url | TEXT | NO | - | 配信URL |
| duration_ms | INTEGER | YES | - | 再生時間(ms) |
| is_active | INTEGER | NO | 0 | 1=このシーンで有効 |
| tenant_id | TEXT | NO | - | テナントID |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:** (child_id, scene)

親が録音・アップロードしたカスタム音声。子供ごと・シーンごとに複数登録可能、1つだけアクティブ。

### character_images

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| type | TEXT | NO | - | hero / normal / effort |
| file_path | TEXT | NO | - | 画像ファイルパス |
| prompt_hash | TEXT | YES | NULL | 生成プロンプトのハッシュ |
| generated_at | TEXT | NO | CURRENT_TIMESTAMP | 生成日時 |

### login_bonuses

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| login_date | TEXT | NO | - | ログイン日（YYYY-MM-DD） |
| rank | TEXT | NO | - | おみくじランク |
| base_points | INTEGER | NO | - | 基本獲得ポイント |
| multiplier | REAL | NO | 1.0 | 連続ログイン倍率 |
| total_points | INTEGER | NO | - | 実際の獲得ポイント |
| consecutive_days | INTEGER | NO | 1 | 連続ログイン日数 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**ユニーク制約:** (child_id, login_date)

**おみくじランク定義:**

| ランク | ポイント | 確率（重み） |
|--------|---------|------------|
| 大大吉 | 20 | 1% |
| 大吉 | 10 | 5% |
| 中吉 | 7 | 15% |
| 小吉 | 5 | 25% |
| 吉 | 3 | 34% |
| 末吉 | 1 | 20% |

**連続ログイン倍率:**

| 連続日数 | 倍率 |
|---------|------|
| 1-2日 | 1.0x |
| 3-6日 | 1.5x |
| 7-13日 | 2.0x |
| 14-29日 | 2.5x |
| 30日以上 | 3.0x |

### season_events

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| code | TEXT | NO | - | イベントコード（一意） |
| name | TEXT | NO | - | イベント名 |
| description | TEXT | YES | - | 説明文 |
| event_type | TEXT | NO | 'seasonal' | 種別（seasonal/monthly/campaign） |
| start_date | TEXT | NO | - | 開始日（YYYY-MM-DD） |
| end_date | TEXT | NO | - | 終了日（YYYY-MM-DD） |
| banner_icon | TEXT | NO | '🎉' | バナーアイコン |
| banner_color | TEXT | YES | - | バナー背景色（CSS） |
| theme_config | TEXT | YES | - | テーマ設定（JSON） |
| reward_config | TEXT | YES | - | 報酬設定（JSON） |
| mission_config | TEXT | YES | - | ミッション設定（JSON） |
| is_active | INTEGER | NO | 1 | 有効フラグ（0/1） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** code

**mission_config JSON例:**
```json
{"type": "total_records", "target": 20, "description": "20回活動を記録しよう"}
```

**reward_config JSON例:**
```json
{"points": 50, "title": "スタートダッシュ達成！", "bonusXp": 100}
```

### child_event_progress

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| event_id | INTEGER | NO | - | FK → season_events.id |
| status | TEXT | NO | 'active' | 状態（active/completed/reward_claimed） |
| progress_json | TEXT | YES | - | 進捗データ（JSON） |
| reward_claimed_at | TEXT | YES | - | 報酬受取日時 |
| joined_at | TEXT | NO | CURRENT_TIMESTAMP | 参加日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (child_id, event_id)
**インデックス:** child_id

### sibling_challenges

きょうだいチャレンジ（協力・競争）のマスタテーブル。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| title | TEXT | NO | - | チャレンジ名 |
| description | TEXT | YES | - | 説明文 |
| challenge_type | TEXT | NO | 'cooperative' | cooperative / competitive |
| period_type | TEXT | NO | 'weekly' | weekly / monthly / custom |
| start_date | TEXT | NO | - | 開始日 |
| end_date | TEXT | NO | - | 終了日 |
| target_config | TEXT | NO | - | 目標設定（JSON: metric, baseTarget, categoryId?, ageAdjustments?） |
| reward_config | TEXT | NO | - | 報酬設定（JSON: points, message?） |
| status | TEXT | NO | 'active' | active / completed / expired |
| is_active | INTEGER | NO | 1 | 有効フラグ |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**インデックス:** status, (start_date, end_date)

### sibling_challenge_progress

きょうだいチャレンジの子供ごとの進捗。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| challenge_id | INTEGER | NO | - | FK → sibling_challenges.id (CASCADE) |
| child_id | INTEGER | NO | - | FK → children.id (CASCADE) |
| current_value | INTEGER | NO | 0 | 現在の進捗値 |
| target_value | INTEGER | NO | - | 年齢調整済み目標値 |
| completed | INTEGER | NO | 0 | 達成フラグ |
| completed_at | TEXT | YES | - | 達成日時 |
| reward_claimed | INTEGER | NO | 0 | 報酬受取フラグ |
| reward_claimed_at | TEXT | YES | - | 報酬受取日時 |
| progress_json | TEXT | YES | - | 拡張用JSON |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (challenge_id, child_id)
**インデックス:** child_id

### sibling_cheers

きょうだい間おうえんスタンプ。定型6種のスタンプを送り合う。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| from_child_id | INTEGER | NO | - | FK → children.id (CASCADE) 送信者 |
| to_child_id | INTEGER | NO | - | FK → children.id (CASCADE) 受信者 |
| stamp_code | TEXT | NO | - | スタンプ種別コード |
| tenant_id | TEXT | NO | 'default' | テナントID |
| sent_at | TEXT | NO | CURRENT_TIMESTAMP | 送信日時 |
| shown_at | TEXT | YES | - | 表示日時（既読マーク） |

**インデックス:** (to_child_id, shown_at)

### push_subscriptions

Web Push 通知の購読情報。1ブラウザ1レコード。

#### #1593 (ADR-0023 I6) subscriber_role 監査

`subscriber_role` カラム (`'parent' | 'owner'`) で購読主体を必ず記録する。`'child'` は API 層で 403 拒否されるため DB に到達しない。COPPA 改正 + ADR-0012 Anti-engagement の二重リスク対策。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| endpoint | TEXT | NO | - | Push API エンドポイントURL（UNIQUE） |
| keys_p256dh | TEXT | NO | - | P256dh 公開鍵（Base64） |
| keys_auth | TEXT | NO | - | Auth シークレット（Base64） |
| user_agent | TEXT | YES | - | 登録時の User-Agent |
| subscriber_role | TEXT | NO | 'parent' | 購読主体ロール (#1593): `'parent' \| 'owner'` のみ。`'child'` は subscribe API で構造的に拒否 (403)。送信側 (`notification-service.ts`) でも二重防御で skip。default は既存レコード backfill 用 (ADR-0031 NULL 混在防止) |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 登録日時 |

**インデックス:** tenant_id

### notification_logs

通知送信ログ。レート制限（3通/日）と監査に使用。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| notification_type | TEXT | NO | - | 通知種別（reminder/streak_warning/achievement/level_up） |
| title | TEXT | NO | - | 通知タイトル |
| body | TEXT | NO | - | 通知本文 |
| sent_at | TEXT | NO | CURRENT_TIMESTAMP | 送信日時 |
| success | INTEGER | NO | 1 | 送信成功フラグ |
| error_message | TEXT | YES | - | エラー内容 |

**インデックス:** (tenant_id, sent_at)

### certificates

がんばり証明書。マイルストーン達成時に自動発行される。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| tenant_id | TEXT | NO | - | テナントID |
| certificate_type | TEXT | NO | - | 証明書種別（streak_7, level_10, monthly_2026-04 等） |
| title | TEXT | NO | - | 証明書タイトル |
| description | TEXT | YES | - | 証明書説明文 |
| issued_at | TEXT | NO | CURRENT_TIMESTAMP | 発行日時 |
| metadata | TEXT | YES | - | 追加データ（JSON: streakDays, level, icon 等） |

**ユニーク制約:** (child_id, tenant_id, certificate_type)

**証明書種別:**
- `streak_N` — N日連続記録（7/14/30/60/100）
- `level_N` — レベルN到達（5/10/20/30/50）
- `monthly_YYYY-MM` — 月間がんばり（月内活動10回以上）
- `category_master_CODE` — カテゴリマスター（★5到達）
- `annual_YYYY` — 年間がんばり大賞

### custom_achievements

カスタム実績。親が子供ごとに作成するオリジナル実績。条件達成時に自動解放＋ボーナスポイント付与。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| child_id | INTEGER | NO | - | FK → children.id |
| name | TEXT | NO | - | 実績名（例: ピアノ100回マスター） |
| description | TEXT | YES | - | 実績説明 |
| icon | TEXT | NO | '🏅' | 表示アイコン（絵文字） |
| condition_type | TEXT | NO | - | 条件タイプ（total_count/activity_count/category_count/streak_days/activity_streak） |
| condition_activity_id | INTEGER | YES | - | 対象活動ID（activity_count/activity_streak 時） |
| condition_category_id | INTEGER | YES | - | 対象カテゴリID（category_count 時） |
| condition_value | INTEGER | NO | - | 目標値 |
| bonus_points | INTEGER | NO | 100 | 達成時ボーナスポイント |
| unlocked_at | TEXT | YES | - | 解放日時 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:** `idx_custom_achievements_tenant_child` ON (tenant_id, child_id)

**プラン別制限:** free=0個, standard=10個, family=無制限

### custom_titles（⚠️ #321 で廃止予定）

カスタム称号。親が子供ごとに作成するオリジナル称号。条件達成で解放、1つだけ装備可能。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| child_id | INTEGER | NO | - | FK → children.id |
| name | TEXT | NO | - | 称号名（例: ピアノのめいじん） |
| icon | TEXT | NO | '📛' | 表示アイコン（絵文字） |
| condition_type | TEXT | NO | - | 条件タイプ（level_reach/achievement_count/activity_count/streak_days） |
| condition_value | INTEGER | NO | - | 目標値 |
| condition_activity_id | INTEGER | YES | - | 対象活動ID（activity_count 時） |
| unlocked_at | TEXT | YES | - | 解放日時 |
| equipped | INTEGER | NO | 0 | 装備中フラグ（0/1） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:** `idx_custom_titles_tenant_child` ON (tenant_id, child_id)

**プラン別制限:** free=0個, standard=5個, family=無制限

### report_daily_summaries

日次バッチ集計の中間テーブル。月次レポート表示時に集約して使用する。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| child_id | INTEGER | NO | - | FK → children.id |
| date | TEXT | NO | - | 集計対象日（YYYY-MM-DD） |
| activity_count | INTEGER | NO | 0 | 当日の活動記録数 |
| category_breakdown | TEXT | NO | '{}' | カテゴリ別集計（JSON） |
| checklist_completion | TEXT | NO | '{}' | チェックリスト達成状況（JSON） |
| level | INTEGER | NO | 1 | 当日時点のレベル |
| total_points | INTEGER | NO | 0 | 当日時点の累計ポイント |
| streak_days | INTEGER | NO | 0 | 当日時点の連続活動日数 |
| new_achievements | INTEGER | NO | 0 | 当日の新規実績獲得数 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**ユニーク制約:** (tenant_id, child_id, date)

**インデックス:**
- `idx_report_daily_child_date` ON (child_id, date)
- `idx_report_daily_tenant_date` ON (tenant_id, date)

**category_breakdown JSON例:**
```json
{"うんどう": 3, "べんきょう": 2, "おてつだい": 1}
```

**checklist_completion JSON例:**
```json
{"completed": 4, "total": 5, "items": ["朝のしたく", "歯みがき", "お片づけ", "あいさつ"]}
```

### cloud_exports

クラウドエクスポート共有。テナント間でデータを共有するためのエクスポート管理テーブル。PINコードで他テナントがインポート可能。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| export_type | TEXT | NO | - | エクスポート種別（'template' / 'full'） |
| pin_code | TEXT | NO | - | 共有用PINコード（UNIQUE） |
| s3_key | TEXT | NO | - | S3上のファイルキー |
| file_size_bytes | INTEGER | NO | - | ファイルサイズ（バイト） |
| label | TEXT | YES | NULL | ラベル（任意の説明文） |
| description | TEXT | YES | NULL | 詳細説明 |
| expires_at | TEXT | NO | - | 有効期限（ISO 8601） |
| download_count | INTEGER | NO | 0 | ダウンロード回数 |
| max_downloads | INTEGER | NO | 10 | 最大ダウンロード回数 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:**
- `idx_cloud_exports_tenant` ON (tenant_id)
- `idx_cloud_exports_pin` ON (pin_code)

### daily_battles

日次バトル。子供ごとに1日1回のバトルを管理する。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| enemy_id | INTEGER | NO | - | 敵マスタID（コード定義） |
| date | TEXT | NO | - | バトル日（YYYY-MM-DD） |
| status | TEXT | NO | 'pending' | 状態（pending/completed） |
| outcome | TEXT | YES | NULL | 結果（win/lose） |
| reward_points | INTEGER | NO | 0 | 獲得報酬ポイント |
| turns_used | INTEGER | NO | 0 | 使用ターン数 |
| player_stats_json | TEXT | NO | '{}' | プレイヤーステータスJSON |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (child_id, date)
**インデックス:** child_id

### enemy_collection

敵図鑑。子供が倒した敵のコレクション記録。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| enemy_id | INTEGER | NO | - | 敵マスタID（コード定義） |
| first_defeated_at | TEXT | NO | CURRENT_TIMESTAMP | 初撃破日時 |
| defeat_count | INTEGER | NO | 1 | 撃破回数 |

**ユニーク制約:** (child_id, enemy_id)
**インデックス:** child_id

### usage_logs

子供の使用時間ログ。15分自動スリープ（#1292）用のセッション記録。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| child_id | INTEGER | NO | - | FK → children.id |
| started_at | TEXT | NO | - | セッション開始日時（ISO8601 UTC） |
| ended_at | TEXT | YES | NULL | セッション終了日時（NULL = 進行中） |
| duration_sec | INTEGER | YES | NULL | 使用時間（秒）。ended_at 設定時に計算 |

**インデックス:** (child_id, started_at), (tenant_id, started_at)

**備考:**
- 子供ページへの訪問で開始、離脱・自動スリープで終了
- ended_at が NULL のレコードは「進行中セッション」を示す
- 管理画面ダッシュボードで本日の子供ごとの使用時間集計に使用

### cancellation_reasons (#1596 / ADR-0023 §3.8 / I3)

解約理由ヒアリング。**全プラン強制**で解約フロー (`/admin/billing/cancel`) で必ず収集される。
PO の解約原因可視化（卒業 vs 離反 vs 中断比率）と改善方向検証用。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID（FK ではないが `tenants.id` に対応） |
| category | TEXT | NO | - | `'graduation'` \| `'churn'` \| `'pause'` の 3 分類 |
| free_text | TEXT | YES | NULL | 自由記述（任意、最大 1000 文字） |
| plan_at_cancellation | TEXT | YES | NULL | 解約時のプラン (free / monthly / yearly / family-monthly / family-yearly / lifetime) |
| stripe_subscription_id | TEXT | YES | NULL | Stripe subscription ID（無料プラン時は NULL） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 解約理由送信日時 |

**インデックス:**
- `idx_cancellation_reasons_tenant` (tenant_id)
- `idx_cancellation_reasons_category_date` (category, created_at) — ops 集計
- `idx_cancellation_reasons_date` (created_at) — 最新順検索

**DynamoDB 対応:**
- PK = `'CANCEL_REASON'` (固定)、SK = `<isoTs>#<uuid>`
- single-partition global（低頻度書込み <100/月想定 → ADR-0010 で過剰防衛 NG、GSI 追加せず）
- テナント単位検索は属性フィルタ Scan（テナント削除時のみのため低頻度）

**Anti-engagement 配慮 (ADR-0012):**
- 「引き止め」UI を出さない（離脱トリガー化を防ぐ）
- 自由記述は任意（義務化はストレス）
- カテゴリは 3 択のみ（細分化すると意思決定コストが上がる）

**ops 利用箇所:**
- `/ops/analytics` の「解約理由集計」セクションで直近 90 日のカテゴリ別件数 + 比率 + 自由記述サンプル（最新 20 件）を表示

**Discord 通知:**
- `cancellation-service.submitCancellationReason()` 呼び出し時に churn channel へカテゴリ + 自由記述含めて即時通知

### titles（⚠️ #321 で廃止予定）

称号マスタ。称号システムは #321 で廃止予定。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| code | TEXT | NO | - | 称号コード（UNIQUE） |
| name | TEXT | NO | - | 称号名 |
| description | TEXT | YES | NULL | 説明文 |
| icon | TEXT | NO | - | アイコン |
| condition_type | TEXT | NO | - | 条件タイプ |
| condition_value | INTEGER | NO | - | 条件値 |
| condition_extra | TEXT | YES | NULL | 追加条件 |
| rarity | TEXT | NO | 'common' | レアリティ |
| sort_order | INTEGER | NO | 0 | 表示順 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

### child_titles（⚠️ #321 で廃止予定）

称号解除履歴。称号システムは #321 で廃止予定。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| title_id | INTEGER | NO | - | FK → titles.id |
| unlocked_at | TEXT | NO | CURRENT_TIMESTAMP | 解放日時 |

**ユニーク制約:** (child_id, title_id)

### achievements

実績マスタ。システム定義の実績一覧。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| code | TEXT | NO | - | 実績コード（UNIQUE） |
| name | TEXT | NO | - | 実績名 |
| description | TEXT | YES | NULL | 説明文 |
| icon | TEXT | NO | - | アイコン |
| category | TEXT | YES | NULL | 実績カテゴリ |
| condition_type | TEXT | NO | - | 条件タイプ |
| condition_value | INTEGER | NO | - | 条件値 |
| bonus_points | INTEGER | NO | - | 達成時ボーナスポイント |
| rarity | TEXT | NO | 'common' | レアリティ |
| sort_order | INTEGER | NO | 0 | 表示順 |
| repeatable | INTEGER | NO | 0 | 繰り返し可能フラグ |
| milestone_values | TEXT | YES | NULL | マイルストーン値（JSON配列） |
| is_milestone | INTEGER | NO | 0 | マイルストーン型フラグ |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

### child_achievements

実績解除履歴。子供が獲得した実績の記録。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| achievement_id | INTEGER | NO | - | FK → achievements.id |
| milestone_value | INTEGER | YES | NULL | マイルストーン値（マイルストーン型の場合） |
| unlocked_at | TEXT | NO | CURRENT_TIMESTAMP | 解放日時 |

**ユニーク制約:** (child_id, achievement_id, milestone_value)

### special_rewards

特別報酬。保護者が子供に与えるマイルストーン報酬。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| granted_by | INTEGER | YES | NULL | 付与者（親のID） |
| title | TEXT | NO | - | 報酬タイトル |
| description | TEXT | YES | NULL | 説明文 |
| points | INTEGER | NO | - | ポイント |
| icon | TEXT | YES | NULL | アイコン |
| category | TEXT | NO | - | 報酬カテゴリ |
| granted_at | TEXT | NO | CURRENT_TIMESTAMP | 付与日時 |
| shown_at | TEXT | YES | NULL | 子供に表示済みの日時 |

**インデックス:** (child_id, granted_at)

### reward_redemption_requests

ごほうびショップ交換申請（#1337）。子供が交換申請し、親が承認/却下する親子連携フロー。
設計詳細: `docs/design/06-UI設計書.md §15`

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id（CASCADE DELETE） |
| reward_id | INTEGER | NO | - | FK → special_rewards.id（RESTRICT: 申請中は削除禁止） |
| requested_at | INTEGER | NO | - | 申請日時（Unix epoch 秒） |
| status | TEXT | NO | 'pending_parent_approval' | 申請状態: `pending_parent_approval` / `approved` / `rejected` / `expired` |
| parent_note | TEXT | YES | NULL | 却下時の親メモ（最大 100 文字）。承認時は NULL |
| resolved_at | INTEGER | YES | NULL | 承認/却下日時（Unix epoch 秒） |
| resolved_by_parent_id | INTEGER | YES | NULL | 承認/却下した親のユーザー内部 ID |
| shown_to_child_at | INTEGER | YES | NULL | 子供への完了/却下通知を表示した日時（NULL = 未表示） |

**インデックス:**
- `(child_id, status)` — 子供の申請一覧クエリ用
- `(reward_id, status)` — 削除時の pending チェック用

**制約:**
- `UNIQUE (child_id, reward_id)` WHERE `status = 'pending_parent_approval'` — 同一報酬への二重申請防止（アプリ層でチェック）
- `reward_id` は RESTRICT 参照（pending 申請が存在する場合は `special_rewards` 削除を API 層で禁止）

**status 遷移:**
```
pending_parent_approval
  → approved     （親が承認）
  → rejected     （親が却下）
  → expired      （30 日経過後、日次 cron が自動移行）
```

**ポイント減算タイミング:** `approved` への遷移時のみ `point_ledger` に記録（type: `'reward_redemption'`）。  
`requested_at` 時点ではポイントを保留しない（シンプルさ優先）。

### checklist_templates

チェックリストテンプレート。子供ごとの持ち物チェックリスト定義。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| name | TEXT | NO | - | テンプレート名 |
| icon | TEXT | NO | '📋' | アイコン |
| points_per_item | INTEGER | NO | 2 | 1項目あたりのポイント |
| completion_bonus | INTEGER | NO | 5 | 全完了ボーナスポイント |
| is_active | INTEGER | NO | 1 | 有効フラグ |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |
| is_archived | INTEGER | NO | 0 | アーカイブフラグ（#783: トライアル終了時に超過リソースを非表示化） |
| archived_reason | TEXT | YES | NULL | アーカイブ理由（例: 'trial_expired'） |

### checklist_template_items

チェックリストアイテム。テンプレート内の個別項目。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| template_id | INTEGER | NO | - | FK → checklist_templates.id |
| name | TEXT | NO | - | 項目名 |
| icon | TEXT | NO | '🏫' | アイコン |
| frequency | TEXT | NO | 'daily' | 頻度（daily/weekday/custom） |
| direction | TEXT | NO | 'bring' | 持ち物方向（bring/return） |
| sort_order | INTEGER | NO | 0 | 表示順 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:** template_id

### checklist_logs

チェックリスト日次記録。日ごとのチェック状況。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| template_id | INTEGER | NO | - | FK → checklist_templates.id |
| checked_date | TEXT | NO | - | チェック日（YYYY-MM-DD） |
| items_json | TEXT | NO | '[]' | チェック済み項目（JSON配列） |
| completed_all | INTEGER | NO | 0 | 全完了フラグ |
| points_awarded | INTEGER | NO | 0 | 付与ポイント |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**ユニーク制約:** (child_id, template_id, checked_date)

### checklist_overrides

チェックリスト日付別アイテム追加/除外。特定日のみの持ち物変更。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| target_date | TEXT | NO | - | 対象日（YYYY-MM-DD） |
| action | TEXT | NO | - | 操作（add/remove） |
| item_name | TEXT | NO | - | 項目名 |
| icon | TEXT | NO | '📦' | アイコン |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:** (child_id, target_date)

### daily_missions

デイリーミッション。日替わりで自動生成されるミッション。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| mission_date | TEXT | NO | - | ミッション日（YYYY-MM-DD） |
| activity_id | INTEGER | NO | - | FK → activities.id |
| completed | INTEGER | NO | 0 | 達成フラグ |
| completed_at | TEXT | YES | NULL | 達成日時 |

**ユニーク制約:** (child_id, mission_date, activity_id)
**インデックス:** (child_id, mission_date)

### child_activity_preferences

子供×活動のピン留め設定。ホーム画面で優先表示する活動。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id (CASCADE) |
| activity_id | INTEGER | NO | - | FK → activities.id (CASCADE) |
| is_pinned | INTEGER | NO | 0 | ピン留めフラグ |
| pin_order | INTEGER | YES | NULL | ピン留め表示順 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (child_id, activity_id)
**インデックス:**
- `idx_child_activity_prefs_child` ON (child_id)
- `idx_child_activity_prefs_pinned` ON (child_id, is_pinned)

### stamp_masters

スタンプマスタ。ログインボーナスで獲得できるスタンプの種類。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| name | TEXT | NO | - | スタンプ名 |
| emoji | TEXT | NO | - | 絵文字 |
| rarity | TEXT | NO | - | レアリティ（N/R/SR/UR） |
| is_default | INTEGER | NO | 1 | デフォルトスタンプフラグ |
| is_enabled | INTEGER | NO | 1 | 有効フラグ |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

### stamp_cards

スタンプカード。週単位のスタンプ収集カード。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id (CASCADE) |
| week_start | TEXT | NO | - | 週の開始日 |
| week_end | TEXT | NO | - | 週の終了日 |
| status | TEXT | NO | 'collecting' | 状態（collecting/redeemable/redeemed） |
| redeemed_points | INTEGER | YES | NULL | 交換ポイント |
| redeemed_at | TEXT | YES | NULL | 交換日時 |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (child_id, week_start)
**インデックス:** child_id

### stamp_entries

スタンプ押印記録。カード上の各スロットに押されたスタンプ。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| card_id | INTEGER | NO | - | FK → stamp_cards.id (CASCADE) |
| stamp_master_id | INTEGER | YES | NULL | FK → stamp_masters.id |
| omikuji_rank | TEXT | YES | NULL | おみくじランク |
| slot | INTEGER | NO | - | スロット番号 |
| login_date | TEXT | NO | - | ログイン日 |
| earned_at | TEXT | NO | CURRENT_TIMESTAMP | 獲得日時 |

**ユニーク制約:**
- (card_id, slot)
- (card_id, login_date)

### activity_mastery

活動別習熟度。各活動の実行回数とレベル。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| activity_id | INTEGER | NO | - | FK → activities.id |
| total_count | INTEGER | NO | 0 | 累計実行回数 |
| level | INTEGER | NO | 1 | 習熟レベル |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** (child_id, activity_id)

### parent_messages

親→子おうえんメッセージ。スタンプ・テキスト・報酬通知の3種別。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| message_type | TEXT | NO | - | メッセージ種別（'stamp' / 'text' / 'reward_notice'） |
| stamp_code | TEXT | YES | - | スタンプ種別コード（stamp の場合） |
| body | TEXT | YES | - | 自由入力テキスト（text の場合） |
| icon | TEXT | NO | '💌' | メッセージアイコン |
| sent_at | TEXT | NO | CURRENT_TIMESTAMP | 送信日時 |
| shown_at | TEXT | YES | - | 子供に表示済みの日時（null=未表示） |

**インデックス:** idx_parent_messages_child(child_id, sent_at), idx_parent_messages_unshown(child_id, shown_at)

### rest_days

おやすみ日。子供ごとに特定日の記録を免除する設定。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| date | TEXT | NO | - | おやすみ日（YYYY-MM-DD） |
| reason | TEXT | NO | 'rest' | 理由（'rest' / 'sick' / 'holiday' 等） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**ユニーク制約:** idx_rest_days_child_date(child_id, date)

### tenant_events

テナント別シーズンイベント参加設定。season_events マスタに対するテナントのオプトイン・カスタマイズ。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| event_code | TEXT | NO | - | イベントコード（season_events.code） |
| year | INTEGER | NO | - | 年度 |
| enabled | INTEGER | NO | 1 | 有効フラグ |
| target_override | TEXT | YES | - | JSON: ミッション上書き設定 |
| reward_memo | TEXT | YES | - | 親が設定する報酬メモ |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** idx_tenant_events_unique(tenant_id, event_code, year)
**インデックス:** idx_tenant_events_tenant_year(tenant_id, year)

### tenant_event_progress

テナントイベントの子供別進捗。tenant_events に対する各子供の達成状況。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| event_code | TEXT | NO | - | イベントコード |
| child_id | INTEGER | NO | - | FK → children.id |
| year | INTEGER | NO | - | 年度 |
| current_count | INTEGER | NO | 0 | 現在の達成カウント |
| completed_at | TEXT | YES | - | 完了日時（null=未完了） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** idx_tenant_event_progress_unique(tenant_id, event_code, child_id, year)
**インデックス:** idx_tenant_event_progress_child(child_id, year)

### auto_challenges

自動生成ウィークリーチャレンジ。毎週月曜に子供ごとに自動生成されるチャレンジ。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| child_id | INTEGER | NO | - | FK → children.id |
| tenant_id | TEXT | NO | - | テナントID |
| week_start | TEXT | NO | - | 週の開始日（YYYY-MM-DD、月曜） |
| category_id | INTEGER | NO | - | FK → categories.id（チャレンジ対象カテゴリ） |
| target_count | INTEGER | NO | - | 目標回数 |
| current_count | INTEGER | NO | 0 | 現在の達成回数 |
| status | TEXT | NO | 'active' | 状態（'active' / 'completed' / 'expired'） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| updated_at | TEXT | NO | CURRENT_TIMESTAMP | 更新日時 |

**ユニーク制約:** idx_auto_challenges_child_week(child_id, week_start)
**インデックス:** idx_auto_challenges_tenant(tenant_id), idx_auto_challenges_status(status)

### trial_history

トライアル履歴。テナントのトライアル開始・終了・ソース・コンバージョン情報を記録。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| start_date | TEXT | NO | - | 開始日（YYYY-MM-DD） |
| end_date | TEXT | NO | - | 終了日（YYYY-MM-DD） |
| tier | TEXT | NO | 'standard' | プラン種別（'standard' / 'family'） |
| source | TEXT | NO | - | ソース（'user_initiated' / 'campaign' / 'admin_grant'） |
| campaign_id | TEXT | YES | - | キャンペーンID（campaign の場合） |
| stripe_subscription_id | TEXT | YES | - | トライアル後に本契約移行した Stripe subscription ID (#769) |
| upgrade_reason | TEXT | YES | - | コンバージョン理由: 'auto' / 'manual' / 'email_cta' (#769) |
| trial_start_source | TEXT | YES | - | トライアル開始トリガー: '/pricing' / '/admin/license' / 'signup_param' (#769) |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |

**インデックス:** idx_trial_history_tenant(tenant_id)

### viewer_tokens

閲覧専用リンクトークン。祖父母等の外部家族向け閲覧用URL。

| カラム | 型 | NULL | デフォルト | 説明 |
|--------|------|------|-----------|------|
| id | INTEGER | NO | autoincrement | 主キー |
| tenant_id | TEXT | NO | - | テナントID |
| token | TEXT | NO | - | ランダムトークン（UNIQUE） |
| label | TEXT | YES | - | ラベル（例: 「おばあちゃん用」） |
| expires_at | TEXT | YES | - | 有効期限（null=無期限） |
| created_at | TEXT | NO | CURRENT_TIMESTAMP | 作成日時 |
| revoked_at | TEXT | YES | - | 無効化日時（null=有効） |

**インデックス:** idx_viewer_tokens_tenant(tenant_id)


---

## 4. Drizzle ORM スキーマ定義（TypeScript）

```typescript
// src/lib/server/db/schema.ts

import { sqliteTable, text, integer, real, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';

export const children = sqliteTable('children', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  nickname: text('nickname').notNull(),
  age: integer('age').notNull(),
  birthDate: text('birth_date'),
  theme: text('theme').notNull().default('pink'),
  createdAt: text('created_at').notNull().default(sql`CURRENT_TIMESTAMP`),
  updatedAt: text('updated_at').notNull().default(sql`CURRENT_TIMESTAMP`),
});

export const activities = sqliteTable('activities', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  name: text('name').notNull(),
  category: text('category').notNull(),
  icon: text('icon').notNull(),
  basePoints: integer('base_points').notNull().default(5),
  ageMin: integer('age_min'),
  ageMax: integer('age_max'),
  isVisible: integer('is_visible').notNull().default(1),
  dailyLimit: integer('daily_limit'),
  sortOrder: integer('sort_order').notNull().default(0),
  isMainQuest: integer('is_main_quest').notNull().default(0),
  createdAt: text('created_at').notNull().default(sql`CURRENT_TIMESTAMP`),
});

export const activityLogs = sqliteTable('activity_logs', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  activityId: integer('activity_id').notNull().references(() => activities.id),
  points: integer('points').notNull(),
  streakDays: integer('streak_days').notNull().default(1),
  streakBonus: integer('streak_bonus').notNull().default(0),
  recordedDate: text('recorded_date').notNull(),
  recordedAt: text('recorded_at').notNull().default(sql`CURRENT_TIMESTAMP`),
  cancelled: integer('cancelled').notNull().default(0),
}, (table) => [
  uniqueIndex('idx_activity_logs_unique_daily').on(table.childId, table.activityId, table.recordedDate),
  index('idx_activity_logs_child_date').on(table.childId, table.recordedDate),
  index('idx_activity_logs_activity').on(table.activityId),
  index('idx_activity_logs_streak').on(table.childId, table.activityId, table.recordedDate),
]);

export const pointLedger = sqliteTable('point_ledger', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  amount: integer('amount').notNull(),
  type: text('type').notNull(),
  description: text('description'),
  referenceId: integer('reference_id'),
  createdAt: text('created_at').notNull().default(sql`CURRENT_TIMESTAMP`),
}, (table) => [
  index('idx_point_ledger_child').on(table.childId, table.createdAt),
]);

export const statuses = sqliteTable('statuses', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  category: text('category').notNull(),
  value: real('value').notNull().default(0.0),
  updatedAt: text('updated_at').notNull().default(sql`CURRENT_TIMESTAMP`),
}, (table) => [
  uniqueIndex('idx_statuses_child_category').on(table.childId, table.category),
]);

export const statusHistory = sqliteTable('status_history', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  category: text('category').notNull(),
  value: real('value').notNull(),
  changeAmount: real('change_amount').notNull(),
  changeType: text('change_type').notNull(),
  recordedAt: text('recorded_at').notNull().default(sql`CURRENT_TIMESTAMP`),
}, (table) => [
  index('idx_status_history_child_cat').on(table.childId, table.category, table.recordedAt),
]);

export const evaluations = sqliteTable('evaluations', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  weekStart: text('week_start').notNull(),
  weekEnd: text('week_end').notNull(),
  scoresJson: text('scores_json').notNull(),
  bonusPoints: integer('bonus_points').notNull().default(0),
  createdAt: text('created_at').notNull().default(sql`CURRENT_TIMESTAMP`),
});

export const marketBenchmarks = sqliteTable('market_benchmarks', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  age: integer('age').notNull(),
  category: text('category').notNull(),
  mean: real('mean').notNull(),
  stdDev: real('std_dev').notNull(),
  source: text('source'),
  updatedAt: text('updated_at').notNull().default(sql`CURRENT_TIMESTAMP`),
}, (table) => [
  uniqueIndex('idx_benchmarks_age_category').on(table.age, table.category),
]);

export const settings = sqliteTable('settings', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),
  updatedAt: text('updated_at').notNull().default(sql`CURRENT_TIMESTAMP`),
});

export const characterImages = sqliteTable('character_images', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  type: text('type').notNull(),
  filePath: text('file_path').notNull(),
  promptHash: text('prompt_hash'),
  generatedAt: text('generated_at').notNull().default(sql`CURRENT_TIMESTAMP`),
});

export const loginBonuses = sqliteTable('login_bonuses', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  childId: integer('child_id').notNull().references(() => children.id),
  loginDate: text('login_date').notNull(),
  rank: text('rank').notNull(),
  basePoints: integer('base_points').notNull(),
  multiplier: real('multiplier').notNull().default(1.0),
  totalPoints: integer('total_points').notNull(),
  consecutiveDays: integer('consecutive_days').notNull().default(1),
  createdAt: text('created_at').notNull().default(sql`CURRENT_TIMESTAMP`),
}, (table) => [
  uniqueIndex('idx_login_bonuses_child_date').on(table.childId, table.loginDate),
]);

export const reportDailySummaries = sqliteTable('report_daily_summaries', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  tenantId: text('tenant_id').notNull(),
  childId: integer('child_id').notNull().references(() => children.id),
  date: text('date').notNull(),
  activityCount: integer('activity_count').notNull().default(0),
  categoryBreakdown: text('category_breakdown').notNull().default('{}'),
  checklistCompletion: text('checklist_completion').notNull().default('{}'),
  level: integer('level').notNull().default(1),
  totalPoints: integer('total_points').notNull().default(0),
  streakDays: integer('streak_days').notNull().default(0),
  newAchievements: integer('new_achievements').notNull().default(0),
  createdAt: text('created_at').notNull().default(sql`CURRENT_TIMESTAMP`),
}, (table) => [
  uniqueIndex('idx_report_daily_unique').on(table.tenantId, table.childId, table.date),
  index('idx_report_daily_child_date').on(table.childId, table.date),
  index('idx_report_daily_tenant_date').on(table.tenantId, table.date),
]);
```

---

## 5. シードデータ

### 初期子供データ

```sql
INSERT INTO children (nickname, age, theme) VALUES
  ('おじょうさま', 4, 'pink');
-- 坊ちゃまは成長後に追加
```

### 初期活動マスタ

要件定義書 §8 の初期活動リスト15件を投入。

### 初期市場ベンチマーク（4歳児・暫定値）

```sql
INSERT INTO market_benchmarks (age, category, mean, std_dev, source) VALUES
  (4, 'うんどう', 30.0, 10.0, '暫定値'),
  (4, 'べんきょう', 20.0, 8.0, '暫定値'),
  (4, 'おてつだい', 25.0, 9.0, '暫定値'),
  (4, 'コミュニケーション', 25.0, 10.0, '暫定値'),
  (4, 'せいかつ', 35.0, 8.0, '暫定値');
```

### 初期ステータス（4歳児・一般平均値で初期化）

```sql
INSERT INTO statuses (child_id, category, value) VALUES
  (1, 'うんどう', 30.0),
  (1, 'べんきょう', 20.0),
  (1, 'おてつだい', 25.0),
  (1, 'コミュニケーション', 25.0),
  (1, 'せいかつ', 35.0);
```

---

## 6. バックアップ方針

| 項目 | 内容 |
|------|------|
| 方式 | SQLiteファイルを日次でgit commit + push |
| タイミング | 毎日 3:00 AM（タスクスケジューラ） |
| 保存先 | GitHub プライベートリポジトリ |
| 保持期間 | Git履歴により永久保持 |
| 復旧手順 | git clone → DBファイル配置 → サービス起動 |

### バックアップスクリプト（参考）

```bash
#!/bin/bash
DB_PATH="/path/to/ganbari-quest.db"
BACKUP_REPO="/path/to/backup-repo"

cp "$DB_PATH" "$BACKUP_REPO/ganbari-quest.db"
cd "$BACKUP_REPO"
git add ganbari-quest.db
git commit -m "backup: $(date +%Y-%m-%d_%H:%M)"
git push origin main
```

---

## 6.5 保持期間ポリシー（プラン別 retention） — #717, #729, #745

本プロジェクトの履歴保持ポリシーの **SSOT（Single Source of Truth）**。
現行の正仕様は [ADR-0028: 物理削除導入（archive）](../decisions/archive/0028-retention-physical-delete.md)。
旧仕様「表示フィルタのみ」の ADR-0027 は #1262 sub-B で削除（git 履歴参照）。

### 6.5.1 プラン別保持期間

| プラン | `historyRetentionDays` | 意味 | 物理削除 |
|--------|----------------------|------|---------|
| free | `90` | 90 日より前のレコードは非表示 + 日次バッチで削除 | **あり（90 日）** |
| standard | `365` | 1 年より前のレコードは非表示 + 日次バッチで削除 | **あり（365 日）** |
| family | `null` | 制限なし（全期間表示） | なし |

値の正は `src/lib/server/services/plan-limit-service.ts` の `PLAN_LIMITS[tier].historyRetentionDays`。設計書とコードが乖離した場合はコードを正として設計書側を修正すること。

### 6.5.2 retention の実装：表示フィルタ + 物理削除（二層構造）

**retention は「読み取り時の表示フィルタ」と「日次物理削除バッチ」の二層で実装する。**

pricing page が約束する「90 日 / 1 年 / 無制限」の履行責務を実データ削除で果たしつつ、
バッチ失敗時のフォールバックとして表示フィルタを残す（多層防御）。

#### 第 1 層：表示フィルタ（`applyRetentionFilter`）

`src/lib/server/services/plan-limit-service.ts`:

| 関数 | 責務 | 呼び出し側 |
|------|------|-----------|
| `getHistoryCutoffDate(tier)` | カットオフ日（YYYY-MM-DD）を返す。`null` なら制限なし | 内部ヘルパ（表示フィルタ / 削除バッチ両方から使用） |
| `applyRetentionFilter(tier, { from, to })` | `from` が cutoff より前なら cutoff で上書きした日付範囲を返す | 履歴一覧 API の呼び出し側 |
| `hasArchivedData(tenantId, childId, tier)` | cutoff より前にデータがあるか判定（アップグレード誘導バナー用） | admin ページ |

**呼び出し側:**

| ファイル | 用途 |
|---------|------|
| `src/routes/(child)/[uiMode=uiMode]/(character)/history/+page.server.ts` | 子供の履歴画面で `applyRetentionFilter` 適用後に `getActivityLogs` を呼ぶ |
| `src/routes/(parent)/admin/children/+page.server.ts` | 管理画面の子供ページで `applyRetentionFilter` + `hasArchivedData` を使用 |
| `src/lib/features/admin/components/ArchiveBanner.svelte` | `hasArchivedData === true` のときアップグレード誘導を表示 |

#### 第 2 層：日次物理削除バッチ（`retention-cleanup-service`）

`src/lib/server/services/retention-cleanup-service.ts`:

| 関数 | 責務 |
|------|------|
| `cleanupExpiredData({ dryRun })` | 全テナントを走査 → プランティア解決 → cutoff 算出 → 各 child の 3 テーブルから `recorded_date < cutoffDate` を物理削除 |

**処理フロー:**

1. `getRepos().auth.listAllTenants()` で全テナントを取得
2. 各テナントについて try/catch で独立処理（1 テナントの失敗が他に波及しない）
3. `deriveLicenseStatus(tenant)` で `licenseStatus` を決定
   - `stripeSubscriptionId` あり + `status=active|grace_period` → `'active'`
   - `stripeSubscriptionId` あり + その他 → `'suspended'`
   - `stripeSubscriptionId` なし → `'none'`
4. `resolveFullPlanTier(tenantId, licenseStatus, planId)` でティアを解決（トライアル考慮、[ADR-0024（archive）](../decisions/archive/0024-plan-tier-resolution-pattern.md)）
5. `getHistoryCutoffDate(tier)` で cutoff 日を算出。`null`（family）ならスキップ
6. `findAllChildren(tenantId)` で子を列挙
7. 各子について以下 3 テーブルから物理削除:
   - `activity_logs` → `repos.activity.deleteActivityLogsBeforeDate(childId, cutoffDate, tenantId)`
   - `point_ledger` → `repos.point.deletePointLedgerBeforeDate(childId, cutoffDate, tenantId)`
   - `login_bonuses` → `repos.loginBonus.deleteLoginBonusesBeforeDate(childId, cutoffDate, tenantId)`
8. 結果 `{tenantsProcessed, tenantsSkipped, childrenProcessed, activityLogsDeleted, pointLedgerDeleted, loginBonusesDeleted, errors[]}` を構造化ログに出力

**エンドポイント:**

`src/routes/api/cron/retention-cleanup/+server.ts`:

| メソッド | 動作 | 認証 |
|---------|------|------|
| `POST` | `{dryRun?: boolean}` — 本番実行 or dry-run | `Authorization: Bearer <CRON_SECRET>` (移行期は `OPS_SECRET_KEY` も後方互換で可 / ADR-0033) |
| `GET` | dry-run 固定（ヘルスチェック用途） | 同上 |

- `CRON_SECRET` / `OPS_SECRET_KEY` のいずれも未設定時は 404 を返してエンドポイントの存在を秘匿
- 認証失敗は 401
- サービス層エラーは 500 + JSON エラーメッセージ

**スケジュール（現状 → 後続チケット）:**

- **現状**: OPS ダッシュボード経由で手動呼び出し（日次目安）
- **後続**: CDK EventBridge Rule で日次自動化（Lambda Web Adapter 経由の変換層が必要）

#### SQLite / DynamoDB 両バックエンドの実装

| バックエンド | activity_logs 削除 | point_ledger 削除 | login_bonuses 削除 |
|------------|------------------|-----------------|------------------|
| SQLite | `db.delete(activityLogs).where(and(eq(childId), lt(recordedDate, cutoff)))` | `lt(createdAt, cutoffDate)`（ISO タイムスタンプは YYYY-MM-DD との辞書順比較で機能） | `lt(loginDate, cutoffDate)` |
| DynamoDB | `Query: PK=childPK AND SK BETWEEN 'LOG#' AND 'LOG#<cutoffDate>'` + BatchWrite（25件ずつ） | 同形式で `POINT#` prefix | 同形式で `LOGIN#` prefix + client-side strict-less-than フィルタ |

DynamoDB の SK 辞書順比較:
- `LOG#<cutoffDate>#<id>` > `LOG#<cutoffDate>` （`#` が追加されるため）→ cutoff 当日のレコードは除外される
- login_bonuses は SK = `LOGIN#<date>` で cutoff 当日が exact match しうるため、client-side で `loginDate < cutoffDate` を追加フィルタ

### 6.5.3 削除対象と非削除対象

| 物理削除する | 理由 |
|-------------|------|
| `activity_logs` | 活動ログ本体。pricing の約束の核心 |
| `point_ledger` エントリ | ポイント履歴の明細 |
| `login_bonuses` | ログインボーナス履歴 |

| 物理削除しない | 理由 |
|---------------|------|
| `point_ledger` の `BALANCE` 集計（DynamoDB）/ 現在残高 | **ポイントは消えず、過去明細だけが消える**。子供の UX 破壊を避けるための #729 の設計判断 |
| `report_daily_summaries` 等の集計テーブル | 月次レポート機能は全期間の集計を前提に動作する。"がんばりの証" は恒久保持 |
| `children` / `activities` / `settings` 等のマスタ | データの意味を破壊する |
| `child_achievements` / `special_rewards` / `stamp_cards` / `stamp_entries` | 現時点では retention 範囲外（将来再検討） |

**ポイント残高の非削除設計（#729 の核心）**:

- `point_ledger` は過去の明細、`BALANCE` は現時点の総残高（`ADD` 演算で更新）
- retention 削除で明細が消えても残高は変わらない
- 結果: 「90 日前にもらったポイントの明細」は消えるが、**そのポイントは今も使える**
- pricing の「90 日履歴保持」の解釈として一貫: 保持されるのは "履歴" であって "ポイントそのもの" ではない

### 6.5.4 集計値は保持期間の影響を受けない

`report_daily_summaries` テーブル（§3 参照）および将来追加される他の集計系テーブルは、プランに関係なく**全期間の集計値を保持**する。

- `applyRetentionFilter` は当該テーブルには**適用しない**
- `retention-cleanup-service` も当該テーブルには**一切触れない**
- 月次レポート機能（有料）は全期間の集計を前提に動作する
- カラム例: `activity_count`, `total_points`, `streak_days`, `new_achievements`

**理由**: 集計値は "がんばりの証" であり、プラン downgrade で失われてはならない。かつ日次 1 行 × 家族数しかなく物理サイズが小さい。

### 6.5.5 プラン downgrade 時の扱い

- downgrade 直後は DB にデータが残っている
- 次回リクエストから `applyRetentionFilter` が cutoff より前のデータを隠す（表示フィルタ層）
- 次の日次バッチ実行時に cutoff より前のデータが物理削除される（削除バッチ層）
- 再 upgrade しても**物理削除されたデータは戻らない**（ADR-0027 からの後退点）
- downgrade 時点の UI 警告（"過去のデータが近日中に削除されます" モーダル）は UI 層の責務
- 削除予告メール（14 日前通知）は後続チケットで実装予定

### 6.5.6 retention と他の物理削除の使い分け

物理削除には以下の種類がある:

| トリガ | 対象 | grace period | 実装 | 備考 |
|-------|------|-------------|------|------|
| **retention cleanup（日次バッチ）** | 各 child の `activity_logs` / `point_ledger` / `login_bonuses` で cutoff より前のレコード | プラン別（90/365 日） | `retention-cleanup-service.cleanupExpiredData()` | 本節の主題。集計・マスタ・残高は触らない |
| 「家族のデータを全部消す」（admin/settings） | children + テナントスコープ全般（認証は残す） | なし | `data-service.clearAllFamilyData()` | テナント・メンバーシップ・招待・認証は残るため、続けて使い続けられる |
| ユーザーによるアカウント削除 | 当該テナントの**全レコード**（認証・テナント本体を含む） | 30 日（[ADR-0022（archive）](../decisions/archive/0022-billing-data-lifecycle-consistency.md)） | `account-deletion-service.deleteOwnerOnlyAccount()` 他 | Stripe を先にキャンセル |
| 違反アカウント強制削除 | 当該テナントの全レコード | なし | OPS 画面からの手動操作 | |

retention（日次小範囲削除）と アカウント削除（全レコード削除）は **別軸** であり、削除範囲を混同しないこと。

### 6.5.7 データ削除スコープマトリクス (#739)

「家族のデータを全部消す」と「アカウント削除」は従来ロジックが分離しており、
削除範囲のズレ（例: `trial_history` が残る / `special_rewards` テンプレートが残る）
がバグの温床だった。共通下位関数を `tenant-cleanup-service.ts` に切り出し、
両方の呼び出し側から同じヘルパを呼ぶ構造に統一する。

**共通下位関数**:

| ヘルパ | 対象 | 実装 |
|-------|------|------|
| `deleteAllChildrenData(tenantId)` | children + child_files + カスケード配下（activity_logs / point_ledger / statuses / status_history / stamp_cards / stamp_entries / child_achievements / login_bonuses / character_images / evaluations / special_rewards / checklist_logs / checklist_overrides） | `tenant-cleanup-service.ts` |
| `deleteTenantScopedData(tenantId)` | activities / viewer_tokens / cloud_exports / push_subscriptions / voice / settings / checklists (templates+items) / daily_missions / evaluations (rest_days) / points / stamp_cards / statuses / login_bonuses / special_rewards (templates) / activity_prefs / activity_mastery / messages / tenant_events / **trial_history** / sibling_challenges / sibling_cheers / auto_challenges / report_daily_summaries / season_events / images | `tenant-cleanup-service.ts` |

**呼び出し側のスコープ**:

| 呼び出し側 | `deleteAllChildrenData` | `deleteTenantScopedData` | テナント/認証レコード |
|------------|:---:|:---:|:---:|
| `clearAllFamilyData` (admin/settings → 家族データ全クリア) | ✅ | ✅ (#739 追加) | ❌（残す） |
| `fullTenantDeletion` (Pattern 1: Owner のみ削除) | ✅ | ✅ | ✅ (削除) |
| `deleteOwnerFullDelete` (Pattern 2b: Owner 全削除) | ✅ | ✅ | ✅ (削除) |
| `transferOwnershipAndLeave` (Pattern 2a: 権限移譲) | ❌ | ❌ | ❌（Owner のみ削除） |
| `deleteChildAccount` (Pattern 3) | 対象子のみ | ❌ | child 行のみ |
| `deleteMemberAccount` (Pattern 4: Viewer/親離脱) | ❌ | ❌ | membership のみ |

**#739 の主要な差分**: `clearAllFamilyData` が従来は children のみを削除していたため、
`trial_history` が残ってトライアル再申込ができない / `special_rewards` テンプレートが
残る等の症状があった。スコープマトリクスの ✅ 欄に `deleteTenantScopedData` を追加
することで 2 箇所のロジックが同じ下位関数を呼ぶ構造になった。

### 6.5.8 アップグレード誘導バナー

`hasArchivedData()` が true を返す家族に対して、`ArchiveBanner.svelte` が
「過去のデータが隠れています。ファミリープランにアップグレードすれば全期間閲覧できます。
ただし削除バッチが走ると物理削除され、その後の再閲覧はできなくなります」という
アップグレード誘導を表示する。

文言は ADR-0028 の物理削除方針に合わせて「削除される前に救済される」ニュアンスに統一すること。

### 6.5.9 禁止事項

- **集計テーブルへの retention 適用禁止**: `report_daily_summaries` 等の集計系には `applyRetentionFilter` / 物理削除バッチを**通さない**
- **retention 対象テーブルの無条件追加禁止**: `retention-cleanup-service` に新しい child 配下テーブルを追加する場合は本 SSOT と ADR-0028 を同時に更新し、削除可能性を再評価すること（特に集計・残高・マスタ）
- **dry-run を省略した本番実行禁止**: 新しい環境・新しいデータ投入の直後は必ず `POST { "dryRun": true }` で削除予定件数を確認してから本実行すること

---

## 7. DynamoDB シングルテーブル設計（本番 SaaS）

### 7.1 テーブル構成

| 項目 | 値 |
|------|-----|
| テーブル名 | `ganbari-quest` |
| パーティションキー | `PK` (String) |
| ソートキー | `SK` (String) |
| 課金モード | オンデマンド |
| 暗号化 | AWS managed key |

### 7.2 GSI（グローバルセカンダリインデックス）

| インデックス名 | PK | SK | 用途 |
|--------------|-----|-----|------|
| GSI1 | `SK` | `PK` | 逆引きクエリ（全テナントの特定 SK を検索） |
| GSI2 | `GSI2PK` | `GSI2SK` | カテゴリ別活動クエリ、時系列クエリ、ライセンスキーのテナント別一覧 |

### 7.3 テナント分離

全テナントスコープエンティティの PK に `T#<tenantId>#` プレフィックスを付与:

```
T#<tenantId>#CHILD#<childId>   // テナント内の子供
T#<tenantId>#ACTIVITY#<actId>  // テナント内の活動
T#<tenantId>#SETTING           // テナント設定
T#<tenantId>#COUNTER           // テナント内 ID カウンタ
```

グローバルエンティティ（CATEGORY, ACHIEVEMENT, TITLE, BENCH）はプレフィックスなし。

### 7.4 エンティティ別キー設計

#### テナントスコープエンティティ

| エンティティ | PK | SK | 説明 |
|------------|-----|-----|------|
| 子供プロフィール | `T#<tid>#CHILD#<cId>` | `PROFILE` | 子供の基本情報 |
| 活動マスタ | `T#<tid>#ACTIVITY#<aId>` | `MASTER` | 活動定義 |
| 活動ログ | `T#<tid>#CHILD#<cId>` | `LOG#<date>#<logId>` | 活動記録 |
| ポイント台帳 | `T#<tid>#CHILD#<cId>` | `POINT#<ts>#<ledgerId>` | ポイント増減記録 |
| ポイント残高 | `T#<tid>#CHILD#<cId>` | `BALANCE` | 集計済み残高 |
| ステータス | `T#<tid>#CHILD#<cId>` | `STATUS#<catId>` | カテゴリ別ステータス値 |
| ステータス履歴 | `T#<tid>#CHILD#<cId>` | `STATHIST#<catId>#<ts>#<id>` | ステータス変更履歴 |
| 実績獲得 | `T#<tid>#CHILD#<cId>` | `ACHV#<achvId>#<milestone>` | 子供の獲得実績 |
| 称号獲得 | `T#<tid>#CHILD#<cId>` | `TITLE#<titleId>` | 子供の獲得称号 |
| ログインボーナス | `T#<tid>#CHILD#<cId>` | `LOGIN#<date>` | 日次ログインボーナス |
| 評価 | `T#<tid>#CHILD#<cId>` | `EVAL#<weekStart>` | 週次評価 |
| 特別報酬 | `T#<tid>#CHILD#<cId>` | `REWARD#<ts>#<rewardId>` | マイルストーン報酬 |
| デイリーミッション | `T#<tid>#CHILD#<cId>` | `MISSION#<date>#<aId>` | 日替わりミッション |
| 活動ピン留め | `T#<tid>#CHILD#<cId>` | `ACTPREF#<aId>` | ピン留め設定 |
| キャラクター画像 | `T#<tid>#CHILD#<cId>` | `IMG#<type>#<hash>` | 生成画像 |
| チェックリストテンプレート | `T#<tid>#CHILD#<cId>` | `CKTPL#<tplId>` | チェックリスト定義 |
| チェックリスト項目 | `T#<tid>#CKTPL#<tplId>` | `ITEM#<sort>#<itemId>` | テンプレート内の項目 |
| チェックリストログ | `T#<tid>#CHILD#<cId>` | `CKLOG#<tplId>#<date>` | 日次チェック記録 |
| チェックリスト上書き | `T#<tid>#CHILD#<cId>` | `CKOVER#<date>#<overId>` | チェック上書き |
| レポート日次集計 | `T#<tid>#CHILD#<cId>` | `RPTDAY#<date>` | 日次バッチ集計（月次レポート用） |
| 設定 | `T#<tid>#SETTING` | `<key>` | テナント設定（キー/値） |
| ID カウンタ | `T#<tid>#COUNTER` | `<entity>` | エンティティ別自動採番 |

#### グローバルエンティティ（テナント横断共有）

| エンティティ | PK | SK | 説明 |
|------------|-----|-----|------|
| カテゴリマスタ | `CATEGORY#<catId>` | `MASTER` | 活動カテゴリ定義 |
| 実績マスタ | `ACHIEVEMENT#<achvId>` | `MASTER` | 実績定義 |
| 称号マスタ | `TITLE#<titleId>` | `MASTER` | 称号定義 |
| 市場ベンチマーク | `BENCH#<age>` | `CAT#<catId>` | 年齢×カテゴリ別平均値 |
| 解約理由 (#1596) | `CANCEL_REASON` | `<isoTs>#<uuid>` | 全テナント横断の解約理由ログ。低頻度書込み (<100/月) のため single-partition で運用、tenant 単位検索は属性 Scan |

#### ライセンスキー関連エンティティ（キーグローバル一意）

ライセンスキー (`GQ-XXXX-XXXX-XXXX-YYYYY`) は全テナントをまたいで一意。そのため PK にテナントプレフィックスは付与せず、キー値そのものをパーティションキーに利用する。詳細は [license-key-lifecycle.md](./license-key-lifecycle.md) を参照。

| エンティティ | PK | SK | 説明 |
|------------|-----|-----|------|
| ライセンスキー本体 | `LICENSE#<licenseKey>` | `LICENSE#<licenseKey>` | status (`active`/`consumed`/`revoked`), plan, tenantId, kind (#801), issuedBy (#801), stripeSessionId, consumedBy, consumedAt, expiresAt (#797), revokedReason, revokedAt, revokedBy (#797), createdAt。**GSI2**: `GSI2PK=TENANT#<tenantId>`, `GSI2SK=LICENSE#<createdAt>` でテナント別一覧検索 (#816) |

**主要属性（LicenseRecord）**:

| 属性 | 型 | 必須 | 説明 |
|------|---|------|------|
| `licenseKey` | string | ○ | キー本体（`GQ-` プレフィックス + 署名付き 22 文字） |
| `tenantId` | string | ○ | 発行先テナント |
| `plan` | enum | ○ | `'monthly' \| 'yearly' \| 'family-monthly' \| 'family-yearly' \| 'lifetime'` |
| `kind` | enum | △ | **#801**: `'purchase' \| 'gift' \| 'campaign'`。未設定は `purchase` 扱い |
| `issuedBy` | string | △ | **#801**: 発行 actor。`gift`/`campaign` では必須 |
| `stripeSessionId` | string | — | Stripe Checkout Session ID（発行元トレース用） |
| `status` | enum | ○ | `'active' \| 'consumed' \| 'revoked'` |
| `consumedBy` | string | — | 消費したユーザー ID |
| `consumedAt` | string (ISO8601) | — | 消費日時 |
| `expiresAt` | string (ISO8601) | △ | **#797**: キーの有効期限（発行から 90 日をデフォルト）。legacy レコードは未設定＝期限なし扱い |
| `revokedReason` | enum | — | `'expired' \| 'leaked' \| 'ops-manual' \| 'refund'` |
| `revokedAt` | string (ISO8601) | — | 失効日時 |
| `revokedBy` | string | — | **#797**: 失効実行者（`ops:<uid>` / `system` / `stripe:<eventId>`） |
| `createdAt` | string (ISO8601) | ○ | 発行日時（90 日期限の起点） |

**検索メソッド (#816)**:

| メソッド | 実装方式 | 用途 |
|---------|---------|------|
| `listLicenseKeysByTenant(tenantId, limit?, cursor?)` | GSI2 Query | テナント別キー一覧（Ops 画面） |
| `listLicenseKeysByStatus(status, limit?, cursor?)` | Scan + Filter | ステータス別一覧（Ops 監視） |
| `listExpiringSoon(days)` | Scan + Filter | N 日以内期限切れ（通知バッチ） |
| `countLicenseKeys(filter?)` | GSI2 Query / Scan (COUNT) | 集計（ダッシュボード） |

**実装リファレンス**: `src/lib/server/services/license-key-service.ts`

**値定数の SSOT (#972)**:
`plan` / `status` / `subscription.status` / `licenseStatus` の値リテラルは以下を Single Source of Truth とし、TypeScript 側の比較・分岐では必ず定数参照で書く（DB 値そのものは変更しない）:

| 値カテゴリ | 定数 | ファイル |
|----------|------|---------|
| ライセンスキープラン (`monthly` / `yearly` / `family-monthly` / `family-yearly` / `lifetime`) | `LICENSE_PLAN` | `src/lib/domain/constants/license-plan.ts` |
| ライセンスキー状態 (`active` / `consumed` / `revoked`) | `LICENSE_KEY_STATUS` | `src/lib/domain/constants/license-key-status.ts` |
| テナント購読状態 (`active` / `grace_period` / `suspended` / `terminated`) | `SUBSCRIPTION_STATUS` | `src/lib/domain/constants/subscription-status.ts` |
| AuthContext.licenseStatus (`active` / `suspended` / `expired` / `none`) | `AUTH_LICENSE_STATUS` | `src/lib/domain/constants/auth-license-status.ts` |

派生集合 (`MONTHLY_PLANS` / `YEARLY_PLANS` / `FAMILY_PLANS` / `ENTITLED_SUBSCRIPTION_STATUSES`) とヘルパ (`planDurationDays` / `isEntitledStatus` 等) を利用して条件分岐を書く。個別リテラル比較は `scripts/check-no-plan-literals.mjs` が CI で自動検出する。

### 7.5 GSI2 の利用パターン

| GSI2PK | GSI2SK | 用途 |
|--------|--------|------|
| `T#<tid>#CAT#<catId>` | `ACT#<sort>#<actId>` | カテゴリ別活動一覧（ソート済み） |
| `TENANT#<tenantId>` | `LICENSE#<createdAt>` | テナント別ライセンスキー一覧（Ops 画面、直近発行順） |

### 7.6 ID 自動採番

DynamoDB には auto-increment がないため、`COUNTER` エンティティで原子的に ID を採番:

```
PK = T#<tenantId>#COUNTER
SK = <entityName>   (例: "child", "activity", "activityLog")
currentId = <最新ID>
```

`counter.ts` の `nextId()` は DynamoDB の `UpdateCommand` + `ADD` で原子的にインクリメント。

### 7.7 認証エンティティ（auth テーブル）

認証関連データは別途のキー構造で管理（`src/lib/server/db/dynamodb/auth-repo.ts`）:

| PK | SK | 説明 |
|-----|-----|------|
| `AUTH#<userId>` | `PROFILE` | ユーザープロフィール（email, cognitoSub） |
| `AUTH#<userId>` | `MEMBER#<tenantId>` | テナントメンバーシップ（role） |
| `TENANT#<tenantId>` | `PROFILE` | テナント情報（プラン, ownerId） |
| `INVITE#<code>` | `INVITE` | 招待コード（tenantId, role, 有効期限） |

### 7.8 バックアップ

- AWS Backup で日次バックアップ（3日保持）
- バックアップ時間: 毎日 18:00 UTC
- PITR は無効（コスト最適化）

---

## 8. スキーマバージョニングと遅延マイグレーション

### 8.1 概要

レコードごとにスキーマバージョン (`_sv`) を付与し、読み取り時に旧バージョンを検出・変換・書き戻す **Lazy Migration** パターンを採用。

| パターン | 役割 |
|---------|------|
| Schema Versioning | レコードごとに `_sv` フィールドでバージョン管理 |
| Lazy Migration (On-Read Upgrade) | 読み取り時に旧バージョンを検出・変換 |
| Write-Back (Optimistic Locking) | 変換結果をDBに書き戻し（DynamoDB: ConditionExpression） |
| Migration Pipeline | V1→V2→V3→...の順序保証された変換チェーン |

### 8.2 `_sv` カラム

対象テーブル: `children`, `statuses`（今後拡大予定）

- SQLite: `_sv INTEGER` カラム（nullable）。既存レコードは `NULL` = V1 扱い
- DynamoDB: `_sv` 属性。既存アイテムは属性なし = V1 扱い
- 新規レコードは挿入時に `_sv: LATEST` を自動付与

### 8.3 ディレクトリ構成

```
src/lib/server/db/migration/
  index.ts           — Public API: hydrate(), withVersion(), getPipeline()
  pipeline.ts        — MigrationPipeline クラス
  registry.ts        — SchemaRegistry（エンティティ別バージョン管理）
  types.ts           — SchemaTransformer インターフェース
  writeback.ts       — DynamoDB Write-Back ヘルパー
  transformers/
    child.ts         — Child V1→V2 (displayConfig, birthdayBonusMultiplier 等のデフォルト保証)
    status.ts        — Status V1→V2 (peakXp のデフォルト保証)
```

### 8.4 エンティティバージョン定義

| エンティティ | LATEST | MIN_SUPPORTED | 変更内容 |
|-------------|--------|---------------|---------|
| child | 2 | 1 | V2: displayConfig, birthdayBonusMultiplier, lastBirthdayBonusYear のデフォルト保証 |
| status | 2 | 1 | V2: peakXp のデフォルト保証（0 かつ totalXp > 0 の場合コピー） |

### 8.5 リポジトリ統合

- **DynamoDB**: 読み取り関数で `hydrate()` → `didMigrate` なら `writeBackDynamoDB()` で Optimistic Locking 書き戻し
- **SQLite**: 読み取り関数で `_sv` チェック → null/旧版なら `UPDATE SET _sv = LATEST`
- **挿入時**: `withVersion()` で `_sv: LATEST` を自動付与

---

## 9. 更新履歴

| 日付 | 版数 | 内容 |
|------|------|------|
| 2026-02-19 | 1.0 | 初版作成（SQLite スキーマ） |
| 2026-03-27 | 2.0 | デュアルバックエンド構成の説明追加、DynamoDB シングルテーブル設計（セクション7）追加 |
| 2026-03-30 | 2.1 | level_titles テーブル追加（#0147 レベル称号カスタマイズ機能） |
| 2026-03-30 | 2.2 | child_custom_voices テーブル追加（#0157 親の声カスタム音声機能） |
| 2026-03-31 | 2.3 | #0257 廃止機能削除に伴い関連記述を除去（DynamoDBエンティティ: 誕生日レビュー、アバターアイテム所持/マスタ、キャリアプラン/履歴、キャリア分野マスタを削除） |
| 2026-04-02 | 2.4 | #0212 season_events・child_event_progress テーブル追加（シーズンイベント基盤） |
| 2026-04-02 | 2.5 | #0216 sibling_challenges・sibling_challenge_progress・sibling_cheers テーブル追加（きょうだいチャレンジ＆おうえんスタンプ） |
| 2026-04-02 | 2.6 | #0218 push_subscriptions・notification_logs テーブル追加（プッシュ通知・リマインダー基盤） |
| 2026-04-03 | 2.7 | report_daily_summaries テーブル追加（日次バッチ集計の中間テーブル、月次レポート用） |
| 2026-04-03 | 2.8 | #0276 certificates テーブル追加（がんばり証明書） |
| 2026-04-03 | 2.9 | #0277 custom_achievements・custom_titles テーブル追加（カスタム実績・称号） |
| 2026-04-03 | 3.0 | #0293 スキーマバージョニング基盤（_sv カラム追加、Migration Pipeline、Lazy Migration + Write-Back） |
| 2026-04-03 | 3.1 | #0294 cloud_exports テーブル追加（クラウドエクスポート共有機能） |
| 2026-04-04 | 4.0 | #344 実装とのスキーマ同期: categories マスタテーブル追加、children に ui_mode/avatar_url/user_id 等追加、activities を category_id FK に修正、achievements/child_achievements/special_rewards/checklist系4テーブル/daily_missions/child_activity_preferences/stamp系3テーブル/activity_mastery/titles/child_titles の定義追加、ER図更新 |
| 2026-04-06 | 4.1 | #549 activities テーブルに is_main_quest カラム追加（メインクエスト機能） |
| 2026-04-10 | 4.2 | #605 daily_battles・enemy_collection テーブル追加（バトルアドベンチャー機能） |
| 2026-04-09 | 4.3 | #609 未記載7テーブル追記（parent_messages, rest_days, tenant_events, tenant_event_progress, auto_challenges, trial_history, viewer_tokens）、廃止テーブル（level_titles, custom_titles）に deprecated 注記追加 |
| 2026-04-11 | 4.4 | #808 ライセンスキー関連エンティティ追加（LicenseRecord, LicenseEvent）+ GSI2 にテナント別ライセンス一覧パターン追加。詳細は [license-key-lifecycle.md](./license-key-lifecycle.md) を参照 |
| 2026-04-11 | 4.5 | #801 LicenseRecord に `kind` / `issuedBy` 追加（cross-tenant consume 制御）。#797 LicenseRecord に `expiresAt` / `revokedBy` 追加（有効期限 90 日 + revoke 監査） |
| 2026-04-12 | 4.6 | #783 children / activities / checklist_templates に is_archived / archived_reason カラム追加（トライアル終了時の超過リソース archive + アップグレード時 restore） |
| 2026-04-12 | 4.6 | #745 §6.5 保持期間ポリシー（プラン別 retention = 表示フィルタ）を正仕様化。実装関数と呼び出し側を明記、集計値除外・物理削除禁止を明文化。旧 ADR-0027（#1262 sub-B で削除）と同期 |
| 2026-04-11 | 4.7 | #739 §6.5.6 データ削除スコープマトリクス追加。`tenant-cleanup-service.ts` へ共通ヘルパ（`deleteAllChildrenData` / `deleteTenantScopedData`）を集約し、`clearAllFamilyData` / `fullTenantDeletion` / `deleteOwnerFullDelete` から同じ下位関数を呼ぶ構造に統一 |
| 2026-04-11 | 4.8 | #717, #729 §6.5 保持期間ポリシーを二層構造（表示フィルタ + 物理削除バッチ）に全面改訂。`retention-cleanup-service` を新設し activity_logs / point_ledger / login_bonuses を日次物理削除。ポイント残高は維持（明細のみ削除）。[ADR-0028（archive）](../decisions/archive/0028-retention-physical-delete.md) 参照。ADR-0027 は supersede |
| 2026-04-12 | 4.9 | #769 trial_history に `stripe_subscription_id` / `upgrade_reason` / `trial_start_source` カラム追加（コンバージョン分析用）。既存レコードは NULL 許容 |
| 2026-04-16 | 5.0 | #972 plan / license / subscription status の値定数を `src/lib/domain/constants/` に集約（`license-plan.ts` / `license-key-status.ts` / `subscription-status.ts` / `auth-license-status.ts`）。DB カラム値は変更せず、比較・分岐ロジックだけを定数経由に統一。`family-monthly` / `family-yearly` / `grace_period` の文字列リテラル直書きは `scripts/check-no-plan-literals.mjs` で CI 自動拒否 |
| 2026-04-25 | 5.1 | #1382 children に `ui_mode_manually_set INTEGER NOT NULL DEFAULT 0` 追加。保護者が明示的に UIMode を設定した場合にフラグを立て、age 変更時の自動再計算をスキップする。`recalcUiMode()` 関数を `age-tier.ts` に追加。`editChild` サービスで flag-aware な再計算ロジックを実装 |
| 2026-04-17 | 5.1 | #816 IAuthRepo にライセンスキー検索メソッド追加（`listLicenseKeysByTenant` / `listLicenseKeysByStatus` / `listExpiringSoon` / `countLicenseKeys`）。ライセンスキー本体に GSI2PK/GSI2SK を付与してテナント別 Query を実現 |
| 2026-04-26 | 5.2 | #1337 `reward_redemption_requests` テーブル追加。ごほうびショップ親子連携フロー（申請→承認→受取）の DB 仕様。status 4値・ポイント減算タイミング・エッジケース（30日期限切れ / 削除 RESTRICT）を定義 |
| 2026-04-26 | 5.3 | #1292 `usage_logs` テーブル追加。子供画面の使用セッション開始・終了・使用時間（秒）を記録し、親ダッシュボードの本日使用時間可視化（Phase 2）に使用。`idx_usage_logs_child_date` / `idx_usage_logs_tenant` インデックス付き |
| 2026-04-28 | 5.4 | #1596 ADR-0023 §3.8 / I3 `cancellation_reasons` テーブル追加。解約フロー (`/admin/billing/cancel`) で全プラン強制収集される 3 分類 (graduation/churn/pause) + 自由記述。DynamoDB は `PK=CANCEL_REASON` single-partition。`idx_cancellation_reasons_tenant` / `_category_date` / `_date` インデックス付き |
| 2026-04-27 | 5.4 | #1593 (ADR-0023 I6) `push_subscriptions` に `subscriber_role TEXT NOT NULL DEFAULT 'parent'` カラム追加。`'parent' \| 'owner'` のみ許容。subscribe API で `child` role を 403 拒否 + `notification-service` 送信時に二重防御で skip。COPPA 改正 + ADR-0012 Anti-engagement 二重リスク対策。詳細は `docs/design/14-セキュリティ設計書.md §8.5` 参照 |
