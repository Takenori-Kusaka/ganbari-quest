## 顧客価値・目的

<!-- SSOT 化 / ファイル移動を伴う refactor PR。並行実装解消・重複ロジック統合などが目的。 -->

**対象ユーザー**: <!-- 開発者（Dev / QM） / システム全体 -->

**解決する課題**: <!-- 散在実装 / 二重管理 / 修正漏れの構造的原因 -->

**期待される効果**: <!-- 修正一箇所で全箇所反映 / 認知負荷低減 / 並行実装ペアの解消 -->

## 関連 Issue

Closes #{{ISSUE_NUMBER}}

## AC 検証マップ (ADR-0004)

{{AC_TABLE}}

## 変更タイプ

- [x] refactor: リファクタリング

## 移動先対応マップ（必須）

<!-- ファイル移動・SSOT 統合時に旧 path → 新 path を全件列挙。
     import 更新件数も併記し、どこか参照漏れがあれば検出可能にする。 -->

| 旧 path | 新 path | 種別 | import / 参照更新件数 |
|---|---|---|---|
| <!-- 例: `src/lib/old/labels.ts` --> | <!-- 例: `src/lib/domain/labels.ts` --> | <!-- 統合 / 移動 / 削除 --> | <!-- 12 件 --> |

**全件確認コマンド**:
```bash
# 旧 path への参照が残っていないことを確認
grep -r "<旧 path>" src/ tests/ site/ --include="*.{ts,svelte,html,js,mjs}"
# → 0 件である必要あり（Skill 経由なら本セクションへ自動転記される）
```

## 影響範囲・横展開チェック

**影響を受ける画面・機能**: <!-- 動作が変わらない場合は「描画変化なし（#1744 ルールに準拠）」と明記 -->

<!-- refactor では並行実装ペアの同期が最重要（`docs/design/parallel-implementations.md`） -->

- [ ] **並行実装ペア**が同期されている（labels SSOT / 5 年齢モード / ナビ 3 種 / E2E・unit seed / チュートリアル）
- [ ] **設計書同期** (ADR-0001): 移動先 path を設計書にも反映した
- [ ] **LP ↔ アプリ整合** (ADR-0013)
- [ ] **並行 PR overlap** 確認 (#1200)

## 「描画変化なし」根拠（#1744）

<!-- refactor で「描画変化なし」を主張する場合、以下を明記。1 文字でも目視差分があれば「描画変化なし」ではない。 -->

- [ ] **ラベル文字列**: 旧と新で完全一致（grep 結果のスナップショット）
- [ ] **HTML 構造**: DOM tree が変わっていない（`scripts/capture.mjs` の DOM HTML スナップショット diff）
- [ ] **CSS class 名**: 変わっていない、または class 名変更が UI に表示されないケース
- [ ] **改行位置 / アイコン / 句読点**: 変わっていない
- [ ] **不可視属性 (`aria-*` / `data-*`)**: 追加していない、または UI 影響なし

「描画変化あり」の場合は本セクションを削除してスクショ 4 スロットに替える。

## テスト・品質セルフチェック

| テスト種別 | コマンド | 結果 |
|---|---|---|
| pre-ready | `npm run pre-ready -- --pr <num>` | <!-- PASS / FAIL --> |
| 旧 path 参照 0 件 | <!-- `grep -r "<旧 path>" src/ tests/ site/` --> | <!-- 0 件 --> |

- [ ] **SOLID**: 単一責任が改善されている / 依存関係が逆方向になっていない
- [ ] **DRY**: 統合により重複が解消されている（解消件数を本文に記載）
- [ ] **YAGNI**: 不要な抽象レイヤーを追加していない
- [ ] **Security**: 移動・統合により秘密情報が露出していない
- [ ] **A11y・パフォーマンス**: 描画変化なしなら a11y 非影響 / 統合によるバンドルサイズ増減を確認

## スクリーンショット / ビジュアルデモ

<!-- 注意: この案内文に opt-out 宣言そのものを書かない (#4255)。テンプレートを消さずに出しただけで
     SS gate が skip されてしまうため、案内は「何を書くか」の説明にとどめる。 -->

UI に到達しない refactor の場合: 「**該当なし**」と書き、括弧内に refactor / docs / chore のいずれかを含む理由を明記する（例: 描画変化なし、根拠は上記セクション）。

UI 描画が変わる場合: 4 スロット必須。

## レビュー依頼事項・破壊的変更

**破壊的変更**:
- [ ] このPRに破壊的変更は**含まれない**（内部 refactor のみ）
- [ ] 含まれる → 公開 API / 設定キー変更を以下に記載

**レビュー依頼事項・QM**:
<!-- 移動先対応マップの完全性 / 「描画変化なし」根拠の妥当性 -->

## 配布済み env / secret (ADR-0006)

- [ ] N/A — 新規 env / secret の追加なし

## Ready for Review チェックリスト

<!-- pre-ready の実行証跡はチェック項目にしない（本リストの未チェックを check-pr-body が block し、その check-pr-body を pre-ready 自身が呼ぶため自己参照 deadlock、#4022）。ログのパスと結果を PR body 本文に記載する。 -->

- [ ] 移動先対応マップが全件埋まっている（旧 path 参照 0 件確認済み）
- [ ] セルフレビュー済み
- [ ] 全 AC が実装済み
- [ ] 「描画変化なし」主張の根拠を明記済み（または UI SS 添付済み）

## QM レビュー結果

[QM 5 手順 approve body は `docs/sessions/qm-session.md` を参照](../docs/sessions/qm-session.md)
