## 顧客価値・目的

<!-- priority:critical のバグ修正。ADR-0002 の 5 要件を満たさない PR は merge 拒否。 -->

**対象ユーザー**: <!-- 子供 / 親（管理者） / 運営 -->

**解決する課題**: <!-- 現に発生している顧客損害 / 不正検知不能 / 監査ログ欠損 / 課金ずれ / データ喪失 -->

**期待される効果**: <!-- 損害停止 / 再発防止メカニズム -->

## 関連 Issue

closes #{{ISSUE_NUMBER}}

<!-- 関連: 直近 30 日の同一ファイル変更 PR を本セクションに列挙（ADR-0002 要件 5） -->

## AC 検証マップ (ADR-0004)

{{AC_TABLE}}

## 変更タイプ

- [x] fix: バグ修正

## ADR-0002 Critical 修正 5 要件チェック（必須）

<!-- priority:critical の場合、以下 5 要件を全て満たすこと。1 つでも未充足なら merge 拒否。 -->

### 要件 1: E2E 回帰テストを同一 PR 内で追加

| AC | テストファイル | テスト内容 |
|---|---|---|
| <!-- 例: AC1 --> | <!-- 例: `tests/e2e/auth-redirect.spec.ts` --> | <!-- 例: 「未認証時の /admin redirect が無限ループしない」 --> |

### 要件 2: AC 全項目完了（部分実装で closes 禁止）

- [ ] Issue の全 AC が本 PR で完了している（残存 TODO / 「予定」なし）

### 要件 3: 提案全実装

- [ ] Issue 本文の「設計方針」「対応案」セクションに記載された全提案を実装した
- [ ] 一部だけ採用した場合は、不採用理由を以下に明記:
  <!-- 例: 提案 B は ADR-0010 Pre-PMF 過剰防衛のため不採用、提案 A のみ実装 -->

### 要件 4: 全 5 年齢モードで実機検証 + スクリーンショット

| 年齢モード | 検証 URL | SS（モバイル / PC） |
|---|---|---|
| baby | <!-- /switch → baby --> | <!-- ![baby-mobile]() / ![baby-pc]() --> |
| preschool | <!-- /switch → preschool --> | <!-- 同上 --> |
| elementary | <!-- /switch → elementary --> | <!-- 同上 --> |
| junior | <!-- /switch → junior --> | <!-- 同上 --> |
| senior | <!-- /switch → senior --> | <!-- 同上 --> |

UI 変更を含まないバグ修正の場合は本表に「**N/A（UI 非影響）+ 影響非影響の根拠**」を明記。

### 要件 5: 直近 30 日の同一ファイル変更 PR チェック

```bash
# 確認コマンド例:
gh pr list --state merged --limit 50 --search "merged:>=$(date -d '30 days ago' +%Y-%m-%d)" \
  --json number,title,mergedAt,files \
  --jq '.[] | select(.files[]?.path | contains("<変更ファイル>"))'
```

| PR | マージ日 | 同一ファイルでの変更内容 | 本 PR との関係 |
|---|---|---|---|
| <!-- 例: #1234 --> | <!-- 2026-04-15 --> | <!-- bug fix --> | <!-- 関連なし / 本 PR で再修正 --> |

該当なしの場合は「**該当なし（直近 30 日に同一ファイル変更 PR なし）**」と明記。

## 影響範囲・変更コンポーネント

**変更レイヤー**:
<!-- 該当する全レイヤーにチェック -->
- [ ] DB スキーマ
- [ ] サービス層
- [ ] API
- [ ] ページ
- [ ] UI コンポーネント
- [ ] その他: <!-- -->

**影響を受ける画面・機能**: <!-- -->

## テスト & 安全装置セルフチェック

- [ ] **`npm run pre-ready -- --pr <num>` 全 Step PASS**
- [ ] **E2E 回帰テストを同一 PR 内で追加**（要件 1、再掲）
- [ ] 追加・変更したテスト一覧:
- [ ] **Critical バグ修正の場合**（ADR-0002）: 上記 5 要件全て確認済み

## スクリーンショット / ビジュアルデモ

UI 変更ありの場合: 4 スロット + 5 年齢モード SS 必須（要件 4 で網羅）。

UI 変更なしの場合: 「**該当なし（バックエンド修正のみ）**」と明記。

## コード品質セルフレビュー (#1481)

- [ ] **DRY**: 同一バグが他ファイルにないか調査済み（ADR-0002 横展開）
- [ ] **YAGNI**: 過剰防衛設計を追加していない（ADR-0010）
- [ ] **Security**: 修正により新たな攻撃面を作っていない
- [ ] **アクセシビリティ**: バグ修正で a11y を毀損していない
- [ ] **パフォーマンス**: 修正でパフォーマンス劣化なし

## 横展開・影響波及チェック

- [ ] 本番アプリ + デモ版を同期
- [ ] 全年齢モード 5 種に横展開
- [ ] ナビゲーション 3 種に反映
- [ ] E2E / ユニットシード同期
- [ ] labels SSOT (ADR-0009)
- [ ] 設計書同期 (ADR-0001)
- [ ] 並行 PR overlap 確認 (#1200)

## レビュー依頼事項・破壊的変更

**破壊的変更**:
- [ ] このPRに破壊的変更は**含まれない**

**レビュー依頼事項・QA**:
<!-- 5 要件のうち特に重点確認してほしい項目 -->

## 配布済み env / secret (ADR-0006)

- [ ] N/A — 新規 env / secret の追加なし

## Ready for Review チェックリスト

- [ ] **`npm run pre-ready -- --pr <num>` 全 Step PASS** をローカル確認した
- [ ] **ADR-0002 5 要件全て満たした**（再掲、自己確認）
- [ ] セルフレビュー済み
- [ ] 全 AC が実装済み
- [ ] UI 変更時: 5 年齢モード SS が GitHub 上で表示確認できる

## QM レビュー結果

[QM 5 手順 approve body は `docs/sessions/qa-session.md` を参照](../docs/sessions/qa-session.md)
