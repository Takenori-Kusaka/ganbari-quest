name: Zenn Lint

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

on:
  push:
    paths:
      - 'docs/zenn/**'
  pull_request:
    paths:
      - 'docs/zenn/**'

# #3494 AC2: repo デフォルト GITHUB_TOKEN 権限への依存を排除するため read-only を明示 (checkout のみ)。
permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - name: Run textlint
        run: npx textlint "docs/zenn/**/*.md"
        continue-on-error: true
